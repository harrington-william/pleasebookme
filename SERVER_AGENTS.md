*This file is used to store Spring Boot server context for AIs*

*Note everything important about the server in here*

# Product invariants — read this first

## Every user IS a tenant. No exceptions, no edge cases.

PleaseBookMe is Calendly for a broader set of service businesses — salons,
rentals, courts, equipment hire. The entire interface is built for **tenant
admins** who manage a service catalog and their bookings. There is no
consumer-side account: attendees book through a widget or public page without
ever holding a platform account.

Two consequences, which override anything written elsewhere in the repo:

1. **A `UserPrincipal` with no tenant is a data-integrity error, not a valid
   state.** It should be treated exactly the way `DefaultUserIdentityLoader`
   already treats a missing Membership or Profile — `orElseThrow`, never
   `orElse(null)`.
2. **The product is unusable without Google Calendar access.** Calendar sync is
   not an optional add-on; it is core to the value proposition, the same way it
   is for Calendly. Onboarding should treat connecting Google as part of signup,
   not as an afterthought in settings.

### ⚠ This contradicts SECURITY.md, which is now stale

`SECURITY.md` → **"Tenant Optionality"** currently states that
`UserPrincipal.tenantUid` is nullable and that "a user without one simply hasn't
subscribed to a plan yet, which is an expected, common state, not an error."
**That is no longer the product rule.**

It has deliberately not been rewritten yet, because the *code* it describes has
not changed either — and right now the stale doc accurately describes the stale
code. The two must be corrected together:

- `DefaultUserIdentityLoader.build()` —
  `tenantRepository.findByOrganizationOrganizationId(...).orElse(null)`
- `UserPrincipal.tenantUid()` — documented as nullable throughout SECURITY.md §5

**Do not flip these to non-null before provisioning actually creates tenants**,
or every existing account — all of which have no tenant row — fails to
authenticate at the loader.

# Workspace provisioning — INCOMPLETE, known gap

`UserProvisioningService` is **not** a finished component. It is one slice of a
planned **Workspace Provisioning Service** that has not been built yet. Any
reading of the codebase that suggests "user provisioning is done" is wrong — it
provisions an *identity*, not a *workspace*.

## What it creates today

`UserProvisioningServiceImpl.provisionUser()` writes five rows:

| Row | Table |
|---|---|
| User | `auth.users` |
| USER role assignment | `auth.user_roles` |
| Organization (`"<name>'s Organization"`) | `organization.organizations` |
| Membership (`accepted = true`) | `organization.memberships` |
| Profile | `organization.profiles` |

## What it does NOT create, and must

- **Tenant — the critical one.** No `tenant.tenants` row is ever written, which
  is precisely why every account today has a null tenant. Every new user must
  receive a tenant on the **`FREE`** plan at provisioning time.
- **Schedule + Availability** (`core.schedules`, `core.availabilities`) — a
  booking product with no default working hours cannot compute a single slot, so
  a freshly provisioned workspace is functionally dead until these exist.
- **Notification preferences** (`notification.notification_preferences`).
- Probably also a default `ResourceType` and a starter `Service`, so a new admin
  lands on a usable catalog instead of an empty one. Decide deliberately before
  building — this is a product call, not a schema call.

## Schema/seed blockers that will stop "every user gets a FREE tenant"

Found by inspection. Resolve these **before** writing provisioning code, because
each one is a `NOT NULL` violation waiting to happen at runtime:

1. **The FREE plan cannot populate a tenant's required limits.**
   `tenant.tenants` requires `max_users`, `max_services`, `max_widgets` (all
   `NOT NULL`). But `V101__drop_plans_not_null.sql` made every `max_*` on
   `tenant.plans` nullable, and `V102__seed_plans.sql` seeds FREE with only two
   of the five:
   ```sql
   INSERT INTO tenant.plans(code, name, price, max_services, max_resources)
   VALUES ('FREE', 'Free plan', 0.00, 10, 10);
   ```
   `max_users`, `max_widgets` and `max_api_keys` are therefore **NULL on the
   FREE plan** and cannot be copied onto the tenant row. Fix by re-seeding FREE
   with all five limits (preferred — keeps the plan the single source of truth),
   or by hardcoding fallbacks in provisioning.

2. **`ecosystem_id` is `NOT NULL`, but only one ecosystem is seeded** —
   `BARBERSHOP`. A rental or court business has nothing valid to point at.
   Needs a broader seed, a neutral `GENERAL`/`UNCATEGORIZED` row, or an
   onboarding step that asks the admin what they run.

3. **`region` is `NOT NULL` with no default** (`AU`/`UK`/`US`/`SG`/`VN`). Must
   be derived at signup — from timezone, locale, or an explicit picker. Vietnam
   is the launch market per the business docs, so `VN` is the likely default.

4. **`status` has no obvious value for a self-serve free signup.** The enum
   offers `ACTIVE`/`TRIAL`/`PENDING`/`SUSPENDED`/`ARCHIVED`. Pick one
   deliberately — `ACTIVE` matches "the free plan is a real plan, not a trial".

5. **`tenants.slug` is `UNIQUE`** and needs the same collision fallback the
   organization slug already has (`UserProvisioningServiceImpl` currently falls
   back to the user UID when the org slug collides).

## Ordering and transactionality

Tenant FKs to organization, owner user, ecosystem and plan — so it must be
written *after* the user and organization are persisted. `register()` is already
`@Transactional`, so keeping the tenant write inside it means a mid-way failure
rolls the entire workspace back rather than stranding a user with no tenant.
Any new provisioning entry point (see the Google one-shot flow below) must
preserve that property.

## ⚠ The Google one-shot flow now depends on this, and it is already built

The **Google one-shot registration flow** (server: `docs/services/auth/GOOGLE_ONE_SHOT_BACKEND.md`,
frontend plan: `client/features/auth/GOOGLE_ONE_SHOT_FLOW.md`) provisions an
account from inside the OAuth callback. It inherits whatever provisioning does
or fails to do — including every gap listed above.

The earlier note here said "do not build that flow before tenant provisioning
exists". It has since been built anyway, deliberately: it routes through the
**same** `UserProvisioningService` via a shared `GoogleAccountResolver` rather
than duplicating provisioning, so it is one code path, not two. Fixing
provisioning still fixes both.

What that means in practice: **every blocker above now breaks registration, not
just calendar sync.** A `NOT NULL` violation surfacing from inside the callback
presents as a `302 …?google=error` and a stack trace in the server log, several
frames from the actual cause. Verify provisioning through the plain
`POST /api/v1/auth/register` endpoint first, where the failure is legible.

# Stack

- Java 21, Spring Boot 4.1, Gradle Kotlin DSL (`server/build.gradle.kts`)
- Jackson 3 — **databind and core** import from `tools.jackson.databind.*` / `tools.jackson.core.*`, NOT `com.fasterxml.jackson.*`. **Annotations are the exception**: `jackson-annotations` is still the 2.x artifact (`com.fasterxml.jackson.core:jackson-annotations:2.21`), so `@JsonProperty`/`@JsonIgnoreProperties`/etc. import from `com.fasterxml.jackson.annotation.*`. Verified against `./gradlew dependencies --configuration compileClasspath` — `tools.jackson.core:jackson-databind:3.1.4` and `com.fasterxml.jackson.core:jackson-annotations:2.21` coexist by design. Don't "fix" an annotation import to `tools.jackson`; that package doesn't exist for annotations.
- Hibernate ORM via `spring-boot-starter-data-jpa`, Jakarta Persistence (`jakarta.persistence.*`)
- Flyway (`flyway-database-postgresql`) owns the schema. Hibernate `ddl-auto` is never used to generate/alter schema — entities are written to match existing `V*__*.sql` migrations exactly, never the other way around.
- No extra ORM libraries (e.g. no hypersistence-utils) — native Postgres enum/JSON mapping is done with plain Hibernate 6.2+ annotations (see below).
- **Redis** via `spring-boot-starter-data-redis` (Lettuce). Pooling is enabled in `application.yaml`, which **requires `org.apache.commons:commons-pool2` on the classpath** — the starter does not bring it transitively, and without it the context fails at startup with `NoClassDefFoundError: GenericObjectPoolConfig` on the `redisConnectionFactory` bean.
- **`RestClient.Builder` is NOT an injectable bean here.** `spring-boot-starter-webclient` supplies `WebClient.Builder`; there is no `RestClient.Builder` auto-configuration on this classpath. Use the static `RestClient.builder()` factory instead (see `DefaultGoogleTokenClient`). Injecting `RestClient.Builder` compiles fine and fails at runtime with `No qualifying bean of type 'RestClient$Builder'`.

# Infrastructure

- `infrastructure/docker/compose.yml` runs `postgres:16`, `redis:7.4-alpine`, and `pgadmin4`. Spring Boot's docker-compose integration starts/stops it automatically (`spring.docker.compose.enabled: true`, `lifecycle-management: start-and-stop`, file path `../infrastructure/docker/compose.yml`).
- **Two separate `.env` files, both gitignored**: `infrastructure/docker/.env` is read by docker compose (interpolates `${REDIS_PASSWORD}` etc. into `compose.yml`); `server/.env` is read by `build.gradle.kts` and injected into `bootRun`/`test` as environment variables. Keys shared by both (e.g. `REDIS_PASSWORD`) **must be kept in sync by hand** — a mismatch surfaces as `WRONGPASS` at runtime, not at startup.
- Redis is password-protected via `--requirepass`, bound to `127.0.0.1` only, `--appendonly yes`, and `--maxmemory-policy noeviction`. The eviction policy is deliberate: the default `allkeys-lru` would silently evict OAuth state entries under memory pressure, turning a valid consent callback into "invalid state" for a real user. Failing writes loudly beats silently dropping security state.
- The redis service carries the label `org.springframework.boot.ignore: "true"`. Without it, `spring-boot-docker-compose` auto-detects the container and injects `RedisConnectionDetails` that **override** `application.yaml` — and since the plain `redis` image exposes no password environment variable, that auto-detected connection has no password and fails. The label keeps `application.yaml` as the single source of truth and makes local behave like production.

# CORS

- Configured in `security/config/WebConfig.java` from `CorsProperties` (`app.cors.*`, validated at startup).
- **Two registrations are required, not one.** `WebMvcConfigurer.addCorsMappings` alone is not enough: Spring Security's filter chain runs *before* the DispatcherServlet, so a preflight never reaches the MVC layer. `WebConfig` therefore also exposes a `CorsConfigurationSource` bean, and `SecurityConfig` calls `.cors(Customizer.withDefaults())` to consult it. Omitting that one line in `SecurityConfig` makes the entire CORS configuration inert.
- Verified behaviour: an allowed origin gets a 200 preflight with full `Access-Control-*` headers; a disallowed origin gets 403 with no `Access-Control-Allow-Origin`.

# Migrations

- Authored with comments in `database/init/<domain>/`, then mirrored comment-free into `server/src/main/resources/db/migration/<domain>/` on request.
- SQL conventions: `BIGSERIAL` PKs, named FK/index constraints, single-line FK/index formatting, zero comments in the Flyway copy.
- **Current applied schema version: v125.**
- **Renumbering a migration below the applied high-water mark breaks the boot.** A previous session renumbered `V112 → V119` while `120/121/122` were already applied, which left a *pending* migration below the current version. Flyway's `validate` rejects that with `Detected resolved migration not applied to database: 119` and the application refuses to start — the failure surfaces as an opaque `entityManagerFactory` bean-creation error, several `Caused by` levels above the real cause. Fixed by renumbering to `V125` (above the mark). Before renumbering anything, check `SELECT MAX(version::numeric) FROM flyway_schema_history WHERE success` and keep pending migrations strictly above it. Since the migration had never been applied, no `flyway_schema_history` rewrite was needed; had it been applied, the ledger would have had to be edited by hand too.

# Package structure

Domain-driven, one folder per subdomain under its bounded context, each with `entity/`, `repository/`, `service/`, `controller/`, `dto/` (created as needed, not all populated upfront):

```
auth/
  user/, role/, permission/, account/, apikey/, attribute/,
  password/, refreshtoken/, rolepermission/, session/, userrole/
  handoff/store/  <- SessionHandoffStore + RedisSessionHandoffStore. No entity,
                     no repository, no table - Redis-backed, mirroring
                     integration/oauthstate/. Don't add a migration.
  enums/          <- auth-schema-specific enums (e.g. AccountStatus)
global/
  enums/          <- cross-schema/public-schema enums (Locale, Theme, WeekStart, Currency)
  exception/, handler/, response/, utils/
security/
  authorization/, config/, identity/, oauth/, permission/, policy/, token/
  crypto/           <- TokenCipher/AesGcmTokenCipher + config/, exception/
  identity/context/ <- CurrentPrincipalProvider (read the principal back out)
  oauth/google/     <- config/, identity/, verifier/, authorization/, client/, exception/
service/
  auth/             <- AuthService, GoogleSignInService, UserProvisioningService,
                       GoogleAccountResolver, SessionIssuer, GoogleOnboardingService
  integration/      <- GoogleConnectService, GoogleAccessTokenProvider, controller/
```

`service/` is the orchestration layer, distinct from the bounded contexts. `service/auth/` and `service/integration/` hold cross-domain business flows (login, register, Google sign-in, Google consent) that coordinate several repositories and the `security/` infrastructure; the bounded-context packages hold per-table CRUD. When adding a flow that spans domains, it belongs under `service/<area>/`, not inside one domain's `service/`.

Other top-level bounded contexts mirror the schemas: `analytics`, `audit`, `billing`, `core`, `customer`, `feature` (appointment/auth/notification/slot/workspace), `integration`, `notification`, `organization`, `resource`, `tenant`, `webhook`, `widget`.

`tenant/` subdomains observed so far: `tenants/`, `ecosystem/`, `plan/`, `domain/`, `enums/` (`TenantStatus`, `TenantRegion`, `EcosystemStatus` — all `tenant`-schema-scoped native Postgres enums).

`organization/` subdomains observed so far: `organizations/`, `profile/`, `membership/`, `membershiprole/` (all four now implemented). `membershiprole/` also has an `id/` folder for its `@EmbeddedId` class — see composite-key note below.

`widget/` subdomains observed so far: `widgets/`, `widgetorigin/`, `enums/` (`WidgetStatus`, `WidgetType` — `widget`-schema-scoped native Postgres enums).

`core/` subdomains observed so far: `schedule/`, `attendee/`, `booking/`, `outofoffice/`, `availability/`, `service/`, `bookingpolicy/`, `selectedslot/`, `enums/` — all now implemented (`ScheduleEntity`, `AvailabilityEntity`, `ServiceEntity`, `BookingEntity`, `BookingPolicyEntity`, `AttendeeEntity`, `SelectedSlotEntity`, `OutOfOfficeEntity`), completing the `core` bounded context. `core/enums/` has `BookingMode` and `BookingStatus`, both wired up as the native-enum-backed `core.booking_mode`/`core.booking_status` Postgres types on `BookingPolicyEntity.bookingMode`/`BookingEntity.status` respectively (unused by `ServiceEntity` — its `period_type` is a plain `VARCHAR`, not a native enum; see below).

`integration/` subdomains: `calendar/`, `sheets/`, `drive/`, `oauthconnection/`, `syncjob/`, `oauthstate/`, `enums/` (`IntegrationType`, `OAuthProvider`, `OAuthConnectionStatus`, `SyncJobStatus`, `SyncJobType` — all `integration`-schema-scoped native Postgres enums). `oauthstate/` is the odd one out: it has **no entity, no repository and no table** — it is Redis-backed (`model/OAuthState`, `store/OAuthStateStore`, `store/RedisOAuthStateStore`), chosen over a Postgres table because `GETDEL` gives single-use consumption in one atomic command and TTL expiry removes the need for a cleanup job entirely. Don't add a migration for it.

`resource/` subdomains — all now implemented: `type/` (`ResourceTypeEntity`), `resources/` (`ResourceEntity`), `pricing/` (`ResourcePricingEntity`), `assignment/` (`ResourceAssignmentEntity`), `calendar/` (`ResourceCalendarEntity`), `maintenance/` (`ResourceMaintenanceEntity`), `attribute/` (`ResourceAttributesEntity`, plus an `id/` folder for its `@EmbeddedId` class — see composite-key note below), `overrides/` (`ResourceOverrideEntity`). `assignment/` briefly had its own `id/` folder with a `ResourceAssignmentId` `@EmbeddedId` stub, deleted once the migration showed it didn't fit a composite key — see the `ResourceAssignmentEntity` note below.

# Entity conventions

Established across `UserEntity`, `RoleEntity`, `PermissionEntity`, `AccountEntity`, `ApiKeyEntity`, `RefreshTokenEntity`, `TenantEntity`, `EcosystemEntity`, `TenantPlanEntity`, `TenantDomainEntity`, `OrganizationEntity`, `MembershipEntity`, `ProfileEntity`, `MembershipRoleEntity`, `WidgetEntity`, `WidgetOriginEntity`, `UserPasswordEntity`, `UserRoleEntity`, `RolePermissionEntity`:

- **PK**: `BigInteger` field named `<entityClassName minus "Entity">Id` (e.g. `userUid`, `roleId`, `permissionId`), mapped to DB column `id`, via `@GeneratedValue(strategy = GenerationType.IDENTITY)` — matches `BIGSERIAL`. The PK name follows the **class name**, not the folder/table name: `tenant.plans` lives under package `tenant/plan/` but the class is `TenantPlanEntity` (disambiguating from a bare `PlanEntity`), so its PK is `tenantPlanId`, not `planId`. Same pattern for `tenant.tenant_domains` under package `tenant/domain/` — class `TenantDomainEntity`, PK `tenantDomainId`, not `domainId`.
- **Timestamps**: `Instant createdAt` / `Instant updatedAt` via `@CreationTimestamp` / `@UpdateTimestamp` (`org.hibernate.annotations`) — matches `TIMESTAMPTZ ... DEFAULT now()`. `createdAt` is `updatable = false`. `Instant` (not `OffsetDateTime`) is the standard choice, matching the platform's canonical-UTC temporal architecture goal.
- **UUID columns** (e.g. `users.uid`): `@UuidGenerator` (`org.hibernate.annotations.UuidGenerator`), `nullable = false, unique = true, updatable = false`. Hibernate generates the UUID client-side; the DB's `gen_random_uuid()` default is not relied on.
- **Postgres native enum columns** (e.g. `public.locale`, `public.theme`, `public.week_start`, `auth.account_status`): map to a Java enum with `@Enumerated(EnumType.STRING)` + `@JdbcTypeCode(SqlTypes.NAMED_ENUM)`. No extra dependency needed — this is native Hibernate 6.2+ support.
- **JSONB columns**: map to `tools.jackson.databind.JsonNode` via `@JdbcTypeCode(SqlTypes.JSON)`.
- **Native Postgres array columns** (e.g. `core.availabilities.days INTEGER[]`): map to the boxed array type (`Integer[]`, not `int[]` or `List<Integer>`) via `@JdbcTypeCode(SqlTypes.ARRAY)` (`org.hibernate.annotations.JdbcTypeCode` / `org.hibernate.type.SqlTypes`) — same annotation family as the JSONB pattern, just a different `SqlTypes` constant. First observed on `AvailabilityEntity.days`.
- **Enum placement**: enums scoped to one Postgres schema and reused only within that schema's tables live in `<schema>/enums/` (e.g. `auth/enums/AccountStatus.java`). Enums under the `public` Postgres schema, shared across domains, live in `global/enums/` (`Locale`, `Theme`, `WeekStart`, `Currency`).
- **Lombok**: `@NoArgsConstructor`, `@AllArgsConstructor`, `@Builder` on every entity. `RoleEntity`/`PermissionEntity`/`AccountEntity` also carry `@Getter`/`@Setter`; `UserEntity` currently does not — flagged here as an observed inconsistency, not a deliberate rule, in case it should be reconciled later.
- Columns with a SQL `DEFAULT` that Hibernate doesn't auto-populate (e.g. plain non-generated NOT NULL columns like `locale`, `theme`, `week_start`, `account_status`, `timezone`) should mirror the DB default as a `@Builder.Default` literal, so entities built via the Lombok builder without setting them don't send an explicit `NULL` that would shadow the DB default and violate `NOT NULL`.
- **Not every table has `created_at`/`updated_at`** — e.g. `auth.accounts` (OAuth provider linkage) has neither; don't add timestamp fields unless the migration actually declares those columns.
- **FK to a schema/entity that doesn't exist yet**: map the raw column as a plain `BigInteger` field (not a relationship) instead of inventing/blocking on an entity. Example still standing: `RefreshTokenEntity.widgetId` — `widget_id` FKs to `widget.widgets(id)` (added via `V63__auth_add_refresh_token_widget_fk.sql`, `ON DELETE SET NULL`) but no `WidgetEntity` exists yet. `owner_user_id` / `user_id` on the same kinds of tables *do* use a real `@ManyToOne` since `UserEntity` already exists.
  - **`ApiKeyEntity.tenant` was upgraded ahead of the normal rule**: `tenant_id` was mapped as a plain `BigInteger` until `TenantEntity` existed, and was then explicitly requested to be upgraded to `@ManyToOne TenantEntity tenant` in the same session `TenantPlanEntity` was implemented — even though `AGENTS.md` still notes api_keys have no functional usage until the tenant module is fully built. The FK relationship can be wired up before the feature that consumes it exists; don't assume "no usage yet" always means "leave as raw BigInteger" — check for an explicit instruction first.
- **`@Builder.Default` should mirror an actual DB `DEFAULT`, not invent business logic** — `AccountEntity.type` was given `@Builder.Default private String type = "oauth"` even though `auth.accounts.type` has no SQL default; this is a deliberate application-level choice made outside this convention, not a migration-mirroring default. Worth knowing this field can diverge from the "mirror the DB default" rule above.
- **`NOT NULL` without a DB default is still a plain required column, not a generated one** — e.g. `refresh_tokens.expires_at` is `TIMESTAMPTZ NOT NULL` with no `DEFAULT`, so `RefreshTokenEntity.expiresAt` is just `@Column(nullable = false)`, not `@CreationTimestamp`/`@UpdateTimestamp` or a `@Builder.Default`. The app is expected to always supply it explicitly.
- **A table may have `created_at` with no `updated_at`** — `auth.refresh_tokens` only has `created_at` (`@CreationTimestamp`, no `@UpdateTimestamp`); tokens are immutable once issued (revoked via `revoked_at`, not row updates). Don't assume the pair always comes together — check the migration.
- **Renamed columns**: `refresh_tokens.client` was renamed to `device_name` in `V76__rename_refresh_tokens_client.sql`. When a later migration renames/alters a column, map the entity to the *current* column name, not the one in the original `CREATE TABLE`.
- **Renamed tables**: `tenant.domains` was renamed to `tenant.tenant_domains` via `V73__rename_tenant_domain.sql` (the original `CREATE TABLE` is still `V25__tenant_domains.sql`). `@Table(schema, name)` must target the post-rename name — check for a later `ALTER TABLE ... RENAME TO` migration before trusting the `CREATE TABLE` file alone.
- **Skeleton files can have copy-paste bugs** — `RefreshTokenEntity`'s stub had `@Table(name = "api_keys")` (leftover from `ApiKeyEntity`). Verify `@Table(schema, name)` against the actual migration filename/table before trusting a pre-existing stub.
- **Multiple unresolved FKs on one table, resolved incrementally**: `TenantEntity` (`tenant.tenants`, `V24__tenant_tenants.sql`) FKs to four different tables. `owner_user_id` → `auth.users` was always a real `@ManyToOne UserEntity` since `UserEntity` already existed. `ecosystem_id` → `tenant.ecosystems`, `plan_id` → `tenant.plans`, and `organization_id` → `organization.organizations` all started as plain `BigInteger`, then were each upgraded — to `@ManyToOne EcosystemEntity ecosystem`, `@ManyToOne TenantPlanEntity plan`, and `@ManyToOne OrganizationEntity organization` respectively — once their target entity got implemented. When a sibling entity for an existing raw-FK field is implemented, go back and upgrade that field to the real relationship rather than leaving it as a `BigInteger`. Apply the "does the target entity exist yet" test per-column, not per-table.
- **`default_locale public.locale NOT NULL DEFAULT 'en'`** (`TenantEntity.defaultLocale`) reuses the shared `global/enums/Locale` enum (same pattern as `UserEntity.locale`) since it's a `public`-schema enum, not a `tenant`-schema one — `@Builder.Default = Locale.en` mirrors the SQL default.
- **`EcosystemEntity`** (`tenant.ecosystems`, `V23__tenant_ecosystems.sql`) is a simple lookup table with no FKs: `code`/`name`/`icon` are plain required `String` columns, `description` is `TEXT NOT NULL` (no `length` set), `status` uses the `tenant`-scoped `EcosystemStatus` enum with `@Builder.Default = EcosystemStatus.REVIEWING` mirroring the SQL `DEFAULT 'REVIEWING'`, and `metadata` is nullable `JsonNode`.
- **`TenantPlanEntity`** (`tenant.plans`, `V22__tenant_plans.sql`) is another FK-free lookup table: `code`/`name` are plain `String`, `price` is `BigDecimal` via `@Column(precision = 10, scale = 2)` matching `NUMERIC(10, 2)`, `currency` reuses the shared `global/enums/Currency` (a `public`-schema enum) with `@Builder.Default = Currency.USD` mirroring the SQL `DEFAULT 'USD'`, the four `max_*` columns are plain `Integer`, and both `features`/`metadata` are nullable `JsonNode`. Its PK is `tenantPlanId` (see the class-name-driven PK rule above), not `planId`.
- **`TenantDomainEntity`** (`tenant.tenant_domains`, originally `V25__tenant_domains.sql`, table renamed by `V73__rename_tenant_domain.sql`): single FK `tenant_id` → `@ManyToOne TenantEntity tenant` since `TenantEntity` already exists. `verified BOOLEAN NOT NULL DEFAULT false` and `is_primary BOOLEAN NOT NULL DEFAULT true` map to `Boolean` fields with `@Builder.Default` mirroring their literal SQL defaults (`isPrimary` for `is_primary`, following the same camelCase-column convention as every other field). `verification_token` is a plain required `String` (`TEXT` column, no `length`).
- **`OrganizationEntity`** (`organization.organizations`, `V18__organization_organizations.sql`) is a root FK-free entity — every other `organization` table (`profiles`, `memberships`) and cross-schema table (`tenant.tenants`, `core.services`, `notification.notifications`, `resource.resource_types`/`resources`, `audit.audit_events`, `customer.customers`) FKs into it, but it FKs to nothing itself. PK is `organizationId`. `name`/`slug` are plain required `String` (`slug` also `unique = true`); `logoUrl`/`bannerUrl`/`bio` are nullable `TEXT` columns with no `length`. `isPrivate BOOLEAN NOT NULL DEFAULT false` and `timezone VARCHAR(100) NOT NULL DEFAULT 'Australia/Sydney'` are `@Builder.Default` mirroring their SQL defaults. `weekStart` reuses the shared `global/enums/WeekStart` (a `public`-schema enum, values `SUNDAY`/`MONDAY`) with `@Builder.Default = WeekStart.MONDAY` mirroring `DEFAULT 'MONDAY'`. `metadata` is nullable `JsonNode`.
- **`MembershipEntity`** (`organization.memberships`, `V20__organization_memberships.sql`) has both its FKs resolved from the start since `OrganizationEntity` and `UserEntity` already existed: `organization_id` → `@ManyToOne OrganizationEntity organization`, `user_id` → `@ManyToOne UserEntity user`. PK is `membershipId`. `accepted BOOLEAN NOT NULL DEFAULT false` is `@Builder.Default`. No `metadata`/JSON columns on this table. `organization.membership_roles` (`V21__organization_membership_roles.sql`) FKs into this table's `id` — once `MembershipRoleEntity` is implemented it should get a real `@ManyToOne MembershipEntity membership`.
- **`ProfileEntity`** (`organization.profiles`, `V19__organization_profiles.sql`) — per-organization user profile (distinct from `MembershipEntity`, which just tracks org membership/acceptance). Both FKs resolved from the start: `user_id` → `@ManyToOne UserEntity user`, `organization_id` → `@ManyToOne OrganizationEntity organization`. PK is `profileId`; `uid UUID NOT NULL DEFAULT gen_random_uuid()` follows the standard `@UuidGenerator` pattern as `profileUid`. `username VARCHAR(100) NOT NULL` is a plain required `String` with `length = 100` (uniqueness is per-organization via `uq_profiles_username_organization`, not enforced at the entity level). No `metadata`/JSON columns on this table.
- **`MembershipRoleEntity`** (`organization.membership_roles`, `V21__organization_membership_roles.sql`) is the first pure join table with a composite PK: `PRIMARY KEY (membership_id, role_id)`, no surrogate `id`. Modeled with `@EmbeddedId` + a `MembershipRoleId` class (`organization/membershiprole/id/MembershipRoleId.java` — composite-key ID classes get their own `id/` package alongside `entity/`), and `@MapsId("membershipId")` / `@MapsId("roleId")` on the two `@ManyToOne` associations (`membership` → `MembershipEntity`, `role` → `RoleEntity`) so the FK columns double as the PK. The `@EmbeddedId` field itself needs `@Builder.Default = new MembershipRoleId()` (not left null) — `@MapsId` populates the embeddable's sub-fields by reflection off the associated entities at flush time, which requires a non-null instance to write into. `created_at` was renamed to `assigned_at` by `V75__rename_membership_roles_created_at.sql`; since the table has only that one timestamp with `DEFAULT now()` and no update counterpart (same shape as `auth.refresh_tokens`), it maps to `@CreationTimestamp` + `updatable = false`, not a `@Builder.Default` literal — `Instant.now()` as a Lombok builder default gets re-evaluated at object-build time (and per Lombok's generated supplier, on every `build()` call that doesn't set it explicitly) rather than at actual persist time, which drifts from the DB-consistent `@CreationTimestamp` pattern used everywhere else. A hand-rolled `@Embeddable` id class also needs real `equals()`/`hashCode()` (composite/`@MapsId` keys require it for Hibernate's identity checks) — write the full field comparison, not just the reference/null/class-mismatch guard clauses; a guard-only `equals()` won't even compile (missing return on the remaining path).
- **`WidgetEntity`** (`widget.widgets`, `V47__widgets.sql`, indexes in `V69__indexes_widget.sql`): `tenant_id` → `@ManyToOne TenantEntity tenant` since `TenantEntity` already exists; `service_id` → `@ManyToOne ServiceEntity service` (`nullable = false`) — originally a plain `BigInteger serviceId` back when the entire `core/` package was still empty, upgraded to a real relationship on explicit instruction once `ServiceEntity` existed, same precedent as `ApiKeyEntity.tenant`. `status` uses the `widget`-scoped `WidgetStatus` enum with `@Builder.Default = WidgetStatus.REGISTERING`, `type` uses `WidgetType` with `@Builder.Default = WidgetType.EMBEDDED`, both mirroring their SQL `DEFAULT`s. `origin_validation BOOLEAN NOT NULL DEFAULT false` is `@Builder.Default`. `public_key`/`secret_key` are plain required `String` (`public_key` also `unique = true`, matching `uq_widgets_public_key`). This table has **three** `TIMESTAMPTZ DEFAULT now()` columns, not the usual pair: `issued_at`, plus the standard `created_at`/`updated_at` at the end (per column-ordering convention). `issued_at` is mapped `@CreationTimestamp` (no `updatable = false`) rather than a `@Builder.Default = Instant.now()` literal, consistent with the project's standing preference (see `MembershipRoleEntity.assignedAt` note above) for `@CreationTimestamp` over Lombok-builder-time `Instant.now()` on any `DEFAULT now()` timestamp column — even a business-semantic one like "issued at" rather than a pure audit column. `updatable = false` was deliberately omitted here (unlike `createdAt`) since `issued_at` is plausibly refreshed on key rotation, not strictly immutable like `created_at`. `last_used_at` is a nullable `Instant` with no default. `uid UUID NOT NULL DEFAULT gen_random_uuid()` follows the standard `@UuidGenerator` pattern as `widgetUid`.
- **`WidgetOriginEntity`** (`widget.widget_origins`, `V48__widget_origins.sql`): `widget_id` → `@ManyToOne WidgetEntity widget` (`nullable = false`); `created_by` → `@ManyToOne UserEntity user` but **nullable**, matching `created_by BIGINT` (no `NOT NULL`) and `ON DELETE SET NULL`. `origin` is a plain required `String`. `verified BOOLEAN NOT NULL DEFAULT false` is `@Builder.Default`. Only `created_at` exists (`@CreationTimestamp`, `updatable = false`) — no `updated_at` column in the migration, same shape as `auth.refresh_tokens`.
- **`ScheduleEntity`** (`core.schedules`, `V26__core_schedules.sql`, index in `V67__indexes_core.sql`): first entity implemented in the `core` bounded context. `user_id` → `@ManyToOne UserEntity user` since `UserEntity` already exists. `title` is a plain required `String`. `timezone VARCHAR(100) NOT NULL DEFAULT 'Australia/Sydney'` is `@Builder.Default` mirroring the SQL default (same default value as `OrganizationEntity.timezone`). Only `created_at`/`updated_at` — no `uid` column on this table. PK is `scheduleId`.
- **`AvailabilityEntity`** (`core.availabilities`, `V27__core_availabilities.sql`, indexes in `V67__indexes_core.sql`): both FKs resolved from the start — `user_id` → `@ManyToOne UserEntity user`, `schedule_id` → `@ManyToOne ScheduleEntity schedule` (now that `ScheduleEntity` exists). `days INTEGER[] NOT NULL` is the first native array column in the codebase — mapped `Integer[] days` via `@JdbcTypeCode(SqlTypes.ARRAY)` (see array-column convention above). `start_time`/`end_time` are `TIME NOT NULL` columns mapped to plain required `java.time.LocalTime` (no default). Only `created_at`/`updated_at` — no `uid` column. PK is `availabilityId`.
- **`ServiceEntity`** (`core.services`, `V28__core_services.sql`, indexes in `V67__indexes_core.sql`, plus two later `ALTER TABLE` migrations): the most FK-heavy `core` table so far. Four FKs, all resolved to real `@ManyToOne` since every target entity already exists: `user_id` → `UserEntity`, `profile_id` → `ProfileEntity`, `organization_id` → `OrganizationEntity`, `schedule_id` → `ScheduleEntity`. `interface_language public.locale NOT NULL DEFAULT 'en'` and `currency public.currency NOT NULL DEFAULT 'USD'` reuse the shared `global/enums` (`Locale`, `Currency`) with `@Builder.Default` mirroring their SQL defaults — same pattern as `TenantEntity`/`TenantPlanEntity`. `period_type VARCHAR(50) NOT NULL DEFAULT 'UNLIMITED'` is a plain `String` with `@Builder.Default`, **not** a native enum — it's a `VARCHAR` column in the migration, not `public.<type>`, even though `core/enums/BookingMode` exists; don't conflate a look-alike scaffolded enum with an actual native-enum column, only the migration's declared column type decides. `min_price`/`max_price NUMERIC(10, 2)` are nullable `BigDecimal` (`precision = 10, scale = 2`, no default, matching `TenantPlanEntity.price`'s type pattern). `requires_confirmation`/`disable_cancelling`/`disable_rescheduling`/`is_instant_service` are all `BOOLEAN NOT NULL DEFAULT false` → `Boolean` with `@Builder.Default = false`. `description`/`location`/`success_redirect_url` are nullable `TEXT`/`String`, `max_active_booking_per_booker` nullable `Integer`, `metadata` nullable `JsonNode`. `destination_calendar_id` (added by `V51__core_add_destination_calendar_fk.sql`) and `destination_sheets_id` (added by `V77__core_add_destination_sheets_fk.sql`) both FK into `integration` schema tables (`ON DELETE SET NULL`, no `NOT NULL`) — originally stayed plain nullable `BigInteger` while no `integration` entity existed, upgraded to real `@ManyToOne DestinationCalendarEntity destinationCalendar` / `@ManyToOne DestinationSheetsEntity destinationSheets` on explicit instruction once `DestinationCalendarEntity`/`DestinationSheetsEntity` were implemented — and per the `createdAt`/`updatedAt`-always-last convention, both are placed **before** `created_at`/`updated_at` in the entity even though Postgres physically appended them after those columns when the `ALTER TABLE`s ran. PK is `serviceId`. Uniqueness (`uq_services_organization_slug` on `(organization_id, slug)`) is DB-level only, same as `ProfileEntity.username` — not enforced at the entity level.
- **`BookingEntity`** (`core.bookings`, `V30__core_bookings.sql`, indexes in `V67__indexes_core.sql`, plus `V51__core_add_destination_calendar_fk.sql` and `V78__rename_booking_cancelation_reason.sql`): the busiest FK table in `core` so far — four separate FKs into `auth.users` alone (`user_id`, `cancelled_by`, `rescheduled_by`, `deleted_by`), all mapped as real `@ManyToOne UserEntity` with distinct field names (`user`, `cancelledBy`, `rescheduledBy`, `deletedBy`) since `UserEntity` already exists; the latter three are nullable (`ON DELETE SET NULL`, no `NOT NULL`) matching the CODING_CONVENTIONS `core.bookings` `uid`-as-external-identifier note: `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) is `@UuidGenerator` as `bookingUid`, same pattern as every other `uid` column. `service_id` → `@ManyToOne ServiceEntity service`. `status core.booking_status NOT NULL DEFAULT 'PENDING'` reuses `core/enums/BookingStatus` (native enum, values match `V3__create_enum_types.sql` exactly) with `@Builder.Default = BookingStatus.PENDING`. `paid`/`rescheduled`/`no_show_host` are `BOOLEAN NOT NULL DEFAULT false` → `Boolean` `@Builder.Default = false`. `idempotency_key`/`description`/`location`/`rejection_reason` are nullable `String`; `deleted_at` is a nullable `Instant` with no default (soft-delete marker, plain column, not `@CreationTimestamp`/`@UpdateTimestamp`). `metadata` nullable `JsonNode`. Same as `ServiceEntity`: `destination_calendar_id`/`destination_sheets_id` (both added by `V51__core_add_destination_calendar_fk.sql`, which — despite its filename — adds both columns to `core.bookings` in one migration) originally stayed plain nullable `BigInteger` (no `integration` entity yet, flagged with a `// Missing destination calendar & sheets` comment above the class), then were upgraded to real `@ManyToOne DestinationCalendarEntity destinationCalendar` / `@ManyToOne DestinationSheetsEntity destinationSheets` on explicit instruction once `DestinationCalendarEntity`/`DestinationSheetsEntity` were implemented — flag comment removed accordingly — and placed before `created_at`/`updated_at` per the always-last convention. PK is `bookingId`. **Rename missed on first pass, corrected afterward**: `cancelation_reason` (as originally spelled in `V30`) was renamed to `cancellation_reason` by `V78__rename_booking_cancelation_reason.sql` — the entity field is `cancellationReason`/`@Column(name = "cancellation_reason")`. The rename migration was present in the repo the whole time and a `grep -rli "core\.bookings\b"` over the migration folder does match it, but it was overlooked on the initial implementation pass and only caught on a later correction — a reminder to re-run the full-table grep (not just skim the first pass's results) before considering a table's migration history closed.
- **`BookingPolicyEntity`** (`core.booking_policies`, `V29__core_booking_policies.sql`): single FK, `service_id` → `@ManyToOne ServiceEntity service`. `booking_mode core.booking_mode NOT NULL DEFAULT 'FIXED'` reuses `core/enums/BookingMode` (native enum) with `@Builder.Default = BookingMode.FIXED`. `duration_type`/`booking_window_type` are both `VARCHAR(50) NOT NULL` with **no** SQL default — plain required `String`, same "required but no `@Builder.Default`" treatment as `RefreshTokenEntity.expiresAt`; don't confuse these with `ServiceEntity.periodType`, which *does* have a SQL default and so does get `@Builder.Default`. `minimum_notice`/`maximum_advance_booking`/`capacity` are `INTEGER NOT NULL` with no default → plain required `Integer`. `default_duration`/`slot_interval`/`before_buffer`/`after_buffer` all have `INTEGER ... DEFAULT` → `Integer` with `@Builder.Default` mirroring their literal SQL defaults (`1`, `30`, `0`, `0`). `minimum_duration`/`maximum_duration` are nullable `Integer`. `allow_overlap`/`allow_multiple_attendee`/`requires_payment` are `BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`; `auto_confirm BOOLEAN NOT NULL DEFAULT true` → `@Builder.Default = true` (first `Boolean` column in the codebase defaulting to `true` rather than `false`). `metadata` nullable `JsonNode`. PK is `bookingPolicyId` (class-name-driven, per the `TenantPlanEntity` precedent).
- **`AttendeeEntity`** (`core.attendees`, `V31__core_attendees.sql`): single FK, `booking_id` → `@ManyToOne BookingEntity booking` (now that `BookingEntity` exists). `phone`/`name` are required `String`; `email`/`timezone` are nullable `String` (no `@Builder.Default`, no SQL default). `locale public.locale` is nullable with **no** `NOT NULL`/`DEFAULT` — unlike every other `Locale`-typed column so far (`UserEntity.locale`, `ServiceEntity.interfaceLanguage`), so it's `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)` but deliberately **without** `@Builder.Default` — first nullable native-enum column in the codebase. `no_show BOOLEAN NOT NULL DEFAULT false` is `@Builder.Default = false`. Only `created_at` — no `updated_at`, no `uid`. PK is `attendeeId`.
- **`SelectedSlotEntity`** (`core.selected_slots`, `V32__core_selected_slots.sql`): two FKs, both resolved — `service_id` → `@ManyToOne ServiceEntity service`, `user_id` → `@ManyToOne UserEntity user`. `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) → `@UuidGenerator selectedSlotUid`, standard pattern. `slot_start`/`slot_end`/`release_at` are all `TIMESTAMPTZ NOT NULL` with no default → plain required `Instant`. `is_seat BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`. Only `created_at` — no `updated_at`. The composite `uq_selected_slots_slot` unique constraint `(service_id, user_id, slot_start, slot_end)` is DB-level only, same treatment as `ServiceEntity`'s `uq_services_organization_slug` — not enforced at the entity level. PK is `selectedSlotId`.
- **`OutOfOfficeEntity`** (`core.out_of_office`, `V33__core_out_of_office.sql`): two FKs, both into `auth.users` — `user_id` → `@ManyToOne UserEntity user`, `to_user_id` → `@ManyToOne UserEntity toUser` (same two-FKs-into-the-same-target-table pattern as `BookingEntity`'s multiple `UserEntity` FKs). `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) → `@UuidGenerator outOfOfficeUid`. `start_time`/`end_time` are required `Instant`; `notes` is nullable `TEXT`/`String`; `reason TEXT NOT NULL` is a required `String` with no `length`. `show_note_publicly BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`. Both `created_at`/`updated_at` present at the end. PK is `outOfOfficeId`.
- **`core` bounded context now fully implemented** — all eight tables (`schedules`, `availabilities`, `services`, `booking_policies`, `bookings`, `attendees`, `selected_slots`, `out_of_office`) have entities.
- **`DestinationCalendarEntity`** (`integration.destination_calendars`, `V49__destination_calendars.sql`, index in `V70__indexes_integration.sql`) and **`DestinationSheetsEntity`** (`integration.destination_sheets`, `V50__destination_sheets.sql`, index in `V70__indexes_integration.sql`) are the first two `integration`-schema entities, both with an identical column shape. Both FKs resolved to real `@ManyToOne` since their targets already exist: `user_id` → `UserEntity`, `service_id` → `ServiceEntity` (both `NOT NULL`, `ON DELETE CASCADE` at the DB level, not mirrored as a JPA cascade). `integration_type integration.integration_type NOT NULL` (no SQL default) reuses the single `integration/enums/IntegrationType` enum across both tables — `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)`, no `@Builder.Default`. `external_id VARCHAR(255) NOT NULL` is a plain required `String` with `length = 255`. Both `created_at`/`updated_at` present at the end, standard `@CreationTimestamp`/`@UpdateTimestamp`. PKs are `destinationCalendarId`/`destinationSheetsId` (class-name-driven, `DestinationSheetsEntity` keeps the plural "Sheets" since that's the class name). **Watch for enum-value drift**: the Flyway migration (`V49`) and its `database/init` source (`V80__destination_calendars.sql`) both declare the Postgres enum literal as `'GOOGLE_SHEET'` (singular), but the `IntegrationType.java` enum constant is `GOOGLE_SHEETS` (plural) — confirmed 2026-07-16 that the live database was manually altered (no migration committed) to rename the Postgres enum label to `GOOGLE_SHEETS` to match the Java code, so the two are currently in sync in the actual DB, but the checked-in `.sql` migration files still say `GOOGLE_SHEET` and do not reflect that manual change. If `NAMED_ENUM` mapping ever throws an unknown-label error, check whether the live DB and the migration files have re-diverged before assuming the Java enum is wrong.
- **Previously-flagged follow-up, now done**: `RefreshTokenEntity.widgetId` (`auth.refresh_tokens.widget_id` → `widget.widgets(id)`, added by `V63__auth_add_refresh_token_widget_fk.sql`, `ON DELETE SET NULL`, no `NOT NULL`) was upgraded from a plain `BigInteger` to a real `@ManyToOne WidgetEntity widget` (nullable) on explicit instruction, alongside the `ServiceEntity`/`BookingEntity` destination-calendar/sheets upgrades and `WidgetEntity.serviceId` → `@ManyToOne ServiceEntity service` in the same batch — the `// Missing widget` comment was removed accordingly. This confirms the `ApiKeyEntity.tenant` precedent: raw FK fields left as `BigInteger` while the target entity didn't exist are expected to be upgraded once it does, but only on explicit instruction, never opportunistically.
- **`UserPasswordEntity`** (`auth.user_passwords`, `V11__auth_user_passwords.sql`) is the first **shared-primary-key** (derived identity) entity in the codebase: `user_id BIGINT PRIMARY KEY` is simultaneously the table's only key and its FK to `auth.users(id)` — there is no surrogate `id` column. Modeled with a plain `@Id @Column(name = "user_id") private BigInteger userId` field plus `@OneToOne @MapsId @JoinColumn(name = "user_id") private UserEntity user` (bare `@MapsId`, no string argument, since the `@Id` field isn't an `@EmbeddedId` — contrast with the composite-key `@MapsId("fieldName")` usage below). `raw TEXT NOT NULL` is a required `String` — per project memory this is intentional plaintext storage during the private-onboarding phase, not a bug to "fix" by hashing/removing it. `hash TEXT NOT NULL` is a required `String`. Both `created_at`/`updated_at` present with the standard `@CreationTimestamp`/`@UpdateTimestamp`.
- **`UserRoleEntity`** (`auth.user_roles`, `V14__auth_user_roles.sql`) and **`RolePermissionEntity`** (`auth.role_permissions`, `V15__auth_role_permissions.sql`) are pure join tables with composite PKs, modeled identically to `MembershipRoleEntity`: `@EmbeddedId` (`UserRoleId`/`RolePermissionId` in their own `id/` packages) with `@Builder.Default = new ...Id()`, and `@MapsId("userId")`/`@MapsId("roleId")` (resp. `@MapsId("roleId")`/`@MapsId("permissionId")`) on the two `@ManyToOne` sides. `user_roles.assigned_at TIMESTAMPTZ NOT NULL DEFAULT now()` (no `updated_at` counterpart) maps to `@CreationTimestamp` + `updatable = false`, same as `MembershipRoleEntity.assignedAt`. `role_permissions` has **no timestamp columns at all** — first join table observed with neither `created_at` nor any other temporal column, so `RolePermissionEntity` only has the `@EmbeddedId` and its two `@ManyToOne` sides. Both of these join-entity classes coexist with the pre-existing `@ManyToMany` collection mappings on `UserEntity.roles` and `RoleEntity.permissions` (which map the same underlying tables implicitly) — this is a deliberate duplication (explicit join entity + implicit `@ManyToMany`), not something to reconcile/remove.
- **`ResourceTypeEntity`** (`resource.resource_types`, `V34__resource_types.sql`) is the first `resource`-schema entity implemented. Single FK, `organization_id` → `@ManyToOne OrganizationEntity organization` (`nullable = false`) since `OrganizationEntity` already exists. `name`/`icon` are required `String` (`length = 100`); `description` is nullable `TEXT`/`String` with no `length`. `metadata` is nullable `JsonNode`. Both `created_at`/`updated_at` at the end, standard `@CreationTimestamp`/`@UpdateTimestamp`. PK is `resourceTypeId`.
- **`ResourceAssignmentEntity`** (`resource.resource_assignments`, `V37__resource_assignments.sql`): **not** a pure join table despite superficially resembling `UserRoleEntity`/`MembershipRoleEntity` (two FKs plus an `assigned_at` timestamp) — the migration gives it its own `id BIGSERIAL PRIMARY KEY` rather than a composite `(resource_id, membership_id)` key, so it's modeled as a standard surrogate-PK entity (`resourceAssignmentId`), **no** `@EmbeddedId`/`ResourceAssignmentId` class. A `ResourceAssignmentId` `@EmbeddedId` stub existed briefly but was deleted once the migration was checked — always confirm a join-shaped table actually has a composite PK (no surrogate `id` column) before reaching for the `@EmbeddedId` pattern; don't infer it from column shape alone. Four FKs total: `resource_id` → `@ManyToOne ResourceEntity resource` (`nullable = false`), `membership_id` → `@ManyToOne MembershipEntity membership` (`nullable = false`), `assigned_by`/`released_by` → `@ManyToOne UserEntity` (both nullable, `ON DELETE SET NULL`, same two-FKs-into-`auth.users`-on-one-table pattern as `BookingEntity`/`OutOfOfficeEntity`). `assigned_at TIMESTAMPTZ NOT NULL DEFAULT now()` → `@CreationTimestamp` + `updatable = false` (no `updated_at` counterpart on this table). `released_at` is nullable `Instant` with no default. `is_primary BOOLEAN NOT NULL DEFAULT true` → `@Builder.Default = true`.
- **`ResourceEntity`** (`resource.resources`, `V35__resources.sql`) is the central `resource`-schema table — three FKs, all resolved: `organization_id` → `@ManyToOne OrganizationEntity organization`, `service_id` → `@ManyToOne ServiceEntity service`, `resource_type_id` → `@ManyToOne ResourceTypeEntity resourceType` (all `nullable = false`). `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) → `@UuidGenerator resourceUid`, standard pattern. `name`/`slug` are required `String` (`length = 255`); `description TEXT NOT NULL` has no `length`. `capacity INTEGER NOT NULL` → required `Integer`. `status VARCHAR(50) NOT NULL` is a plain required `String`, **not** a native enum — same "look-alike but actually a `VARCHAR`" treatment as `ServiceEntity.periodType`. `is_bookable BOOLEAN NOT NULL DEFAULT true` / `is_virtual BOOLEAN NOT NULL DEFAULT false` are `@Builder.Default` mirroring their literal SQL defaults (first table with a `true`-defaulting boolean alongside a `false`-defaulting one). `metadata` nullable `JsonNode`. Both `created_at`/`updated_at` at the end. PK is `resourceId`. `uq_resources_organization_slug` on `(organization_id, slug)` is DB-level only, same treatment as `ServiceEntity.uq_services_organization_slug`.
- **`ResourcePricingEntity`** (`resource.resource_pricing`, `V36__resource_pricing.sql`): single FK, `resource_id` → `@ManyToOne ResourceEntity resource`. `price NUMERIC(10, 2) NOT NULL` → required `BigDecimal` (`precision = 10, scale = 2`), same shape as `TenantPlanEntity.price`. `currency public.currency NOT NULL DEFAULT 'USD'` reuses the shared `global/enums/Currency` with `@Builder.Default = Currency.USD`, same pattern as `ServiceEntity.currency`. `effective_from TIMESTAMPTZ NOT NULL` is a required `Instant` with no default; `effective_until` is nullable `Instant`. Both `created_at`/`updated_at` at the end. PK is `resourcePricingId`.
- **`ResourceCalendarEntity`** (`resource.resource_calendars`, `V38__resource_calendars.sql`): two FKs, both resolved — `resource_id` → `@ManyToOne ResourceEntity resource`, `schedule_id` → `@ManyToOne ScheduleEntity schedule`. No other columns besides both `created_at`/`updated_at`. PK is `resourceCalendarId`.
- **`ResourceMaintenanceEntity`** (`resource.resource_maintenance`, `V39__resource_maintenance.sql`): single FK, `resource_id` → `@ManyToOne ResourceEntity resource`. `start_time`/`end_time TIMESTAMPTZ NOT NULL` are required `Instant`; `reason TEXT NOT NULL` required `String` with no `length`; `status VARCHAR(50) NOT NULL` is a plain required `String` (not a native enum, same treatment as `ResourceEntity.status`). Both `created_at`/`updated_at` at the end. PK is `resourceMaintenanceId`.
- **`ResourceOverrideEntity`** (`resource.resource_overrides`, `V41__resource_overrides.sql`): identical shape to `ResourceMaintenanceEntity` minus the `status` column — single FK `resource_id` → `@ManyToOne ResourceEntity resource`, required `start_time`/`end_time` `Instant`, required `reason TEXT` `String`, both `created_at`/`updated_at` at the end. PK is `resourceOverrideId`.
- **`ResourceAttributesEntity`** (`resource.resource_attributes`, `V40__resource_attributes.sql`, `created_at`/`updated_at` added later by `V74__add_resource_attributes_created_updated.sql`) is a genuine composite-PK table — `PRIMARY KEY (resource_id, key)`, no surrogate `id` column, unlike `ResourceAssignmentEntity` above. Modeled with `@EmbeddedId` + `ResourceAttributesId` (`resource/attribute/id/ResourceAttributesId.java`) holding `resourceId`/`key`, plus `@ManyToOne @MapsId("resourceId") @JoinColumn(name = "resource_id") ResourceEntity resource` — same `@EmbeddedId`/`@MapsId` shape as `MembershipRoleEntity`/`UserRoleEntity`, except only one of the two PK columns (`resource_id`) is an FK; the other (`key VARCHAR(100) NOT NULL`) is a plain value column that lives solely inside the embeddable (no duplicate field on the entity itself) since it isn't a relationship. `value TEXT NOT NULL` is a plain required `String` on the entity. Because `V74` added `created_at`/`updated_at` via `ALTER TABLE` (both `DEFAULT now()`) well after the original `V40` `CREATE TABLE`, they're still placed at the end of the entity per the always-last convention, mapped as standard `@CreationTimestamp`/`@UpdateTimestamp` despite arriving via a later migration.
- **`resource` bounded context now fully implemented** — all eight tables (`resource_types`, `resources`, `resource_pricing`, `resource_assignments`, `resource_calendars`, `resource_maintenance`, `resource_attributes`, `resource_overrides`) have entities.
- **`CustomerEntity`** (`customer.customers`, `V42__customers.sql`, indexes in `V68__indexes_customer.sql`) is the first `customer`-schema entity implemented. Two FKs, both resolved to real `@ManyToOne` since their targets already exist: `tenant_id` → `TenantEntity`, `organization_id` → `OrganizationEntity` (both `nullable = false`). `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) → `@UuidGenerator customerUid`, standard pattern. `email VARCHAR(255)`/`phone VARCHAR(50)`/`name VARCHAR(255)` are required `String` with matching `length`. `avatar_url TEXT` is nullable `String` with no `length`. `locale public.locale` is nullable with **no** `NOT NULL`/`DEFAULT` — reuses shared `global/enums/Locale` as `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)` deliberately **without** `@Builder.Default`, same treatment as `AttendeeEntity.locale`. `timezone VARCHAR(100)` is nullable with no default — unlike `OrganizationEntity.timezone`/`ScheduleEntity.timezone` (both `NOT NULL DEFAULT`), this one is optional per-customer so it's a plain nullable `String`, no `@Builder.Default`. `birthday DATE NOT NULL` is a required `java.time.LocalDate` (first `DATE`-typed column in the codebase, distinct from the `TIMESTAMPTZ`-backed `Instant` columns used everywhere else). `gender VARCHAR(20)` is nullable `String`. `status VARCHAR(50) NOT NULL` is a plain required `String`, **not** a native enum — same look-alike treatment as `ResourceEntity.status`. `marketing_consent BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`. `notes TEXT` nullable `String`, `metadata JSONB` nullable `JsonNode`. Both `created_at`/`updated_at` at the end. PK is `customerId`. `uq_customers_tenant_email`/`uq_customers_tenant_phone` (composite uniqueness on `(tenant_id, email)`/`(tenant_id, phone)`) are DB-level only, same treatment as `ServiceEntity.uq_services_organization_slug` — not enforced at the entity level.
- **`CustomerNoteEntity`** (`customer.customer_notes`, `V43__customer_notes.sql`): two FKs — `customer_id` → `@ManyToOne CustomerEntity customer` (`nullable = false`), `author_user_id` → `@ManyToOne UserEntity authorUser` but **nullable** (`ON DELETE SET NULL`, no `NOT NULL`), same treatment as `WidgetOriginEntity.createdBy`. `content TEXT NOT NULL` is a required `String` with no `length`. Only `created_at` — no `updated_at`, same shape as `auth.refresh_tokens`, so `@CreationTimestamp` + `updatable = false`. PK is `customerNoteId`.
- **`CustomerActivityEntity`** (`customer.customer_activities`, `V44__customer_activities.sql`): single FK, `customer_id` → `@ManyToOne CustomerEntity customer` (`nullable = false`). `activity_type`/`reference_type VARCHAR(50) NOT NULL` and `reference_uid VARCHAR(255) NOT NULL` are plain required `String` with matching `length`. `description TEXT` is nullable `String` with no `length`; `metadata JSONB` nullable `JsonNode`. Only one timestamp column, but named `occurred_at` rather than `created_at` — still `TIMESTAMPTZ NOT NULL DEFAULT now()` with no update counterpart, so mapped `@CreationTimestamp` + `updatable = false` onto the `occurredAt` field (column name follows the migration, not the usual `created_at` convention — an activity-log entry is immutable once recorded, same rationale as `MembershipRoleEntity.assignedAt`/`WidgetEntity.issuedAt` being `@CreationTimestamp`-backed non-`created_at` columns). PK is `customerActivityId`.
- **`CustomerSourceEntity`** (`customer.customer_sources`, `V46__customer_sources.sql`): single FK, `customer_id` → `@ManyToOne CustomerEntity customer` (`nullable = false`). `source VARCHAR(50) NOT NULL` is a plain required `String`. Only `created_at` — no `updated_at`. PK is `customerSourceId`.
- **`CustomerTagEntity`** (`customer.customer_tags`, `V45__customer_tags.sql`): single FK, `customer_id` → `@ManyToOne CustomerEntity customer` (`nullable = false`). `tag VARCHAR(50) NOT NULL` is a plain required `String`. Both `created_at`/`updated_at` present (unlike its `customer_activities`/`customer_sources`/`customer_notes` siblings, which only have one timestamp column each). PK is `customerTagId`. `uq_customer_tags_customer_tag` on `(customer_id, tag)` is DB-level only, not enforced at the entity level.
- **`customer` bounded context now fully implemented** — all five tables (`customers`, `customer_notes`, `customer_activities`, `customer_tags`, `customer_sources`) have entities.
- **`NotificationChannelEntity`** (`notification.notification_channels`, `V52__notification_channels.sql`) is the first `notification`-schema entity implemented — a small FK-free lookup table. `code VARCHAR(50) NOT NULL` is a required `String` with `unique = true` (matching `uq_notification_channels_code`); `name VARCHAR(100) NOT NULL` is a plain required `String`. `enabled BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`. Only `created_at TIMESTAMPTZ NOT NULL DEFAULT now()` — no `updated_at` on this table, same shape as `auth.refresh_tokens`/`organization.membership_roles.assigned_at`, so it's `@CreationTimestamp` + `updatable = false`. PK is `notificationChannelId`.
- **`NotificationTemplateEntity`** (`notification.notification_templates`, `V53__notification_templates.sql`): single nullable FK, `tenant_id BIGINT` (no `NOT NULL`) → `@ManyToOne TenantEntity tenant` (`nullable = true`, `ON DELETE CASCADE` at the DB level, not mirrored). `code VARCHAR(100)`/`name VARCHAR(255)` are required `String` with matching `length` (composite uniqueness `uq_notification_templates_tenant_code` on `(tenant_id, code)` is DB-level only, same treatment as `ServiceEntity.uq_services_organization_slug`). `channel VARCHAR(50) NOT NULL` is a plain required `String`, **not** a native enum — same look-alike-but-actually-`VARCHAR` treatment as `ServiceEntity.periodType`/`ResourceEntity.status`. `subject_template TEXT` is nullable `String` with no `length`; `body_template TEXT NOT NULL` is required `String`. `locale public.locale NOT NULL` (no SQL default) reuses the shared `global/enums/Locale` as `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)` but **without** `@Builder.Default` since there's no literal default to mirror — required-but-no-default treatment, same as `BookingPolicyEntity.durationType`. `enabled BOOLEAN NOT NULL DEFAULT false` → `@Builder.Default = false`; `version INTEGER NOT NULL DEFAULT 1` → `@Builder.Default = 1`. Both `created_at`/`updated_at` at the end. PK is `notificationTemplateId`.
- **`NotificationEntity`** (`notification.notifications`, `V54__notifications.sql`) is the central `notification`-schema table — four FKs, all resolved to real `@ManyToOne` since every target already exists: `tenant_id` → `TenantEntity`, `organization_id` → `OrganizationEntity`, `template_id` → `NotificationTemplateEntity`, `channel_id` → `NotificationChannelEntity` (all `nullable = false`). `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`) → `@UuidGenerator notificationUid`, standard pattern. `recipient_type notification.recipient_type NOT NULL` (no default) reuses `notification/enums/RecipientType` — `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)`, required, no `@Builder.Default` (same required-but-no-default treatment as `NotificationTemplateEntity.locale`). `recipient_uid VARCHAR(255) NOT NULL` is a plain required `String` — despite the column name, it's a free-form external identifier string, not a relationship/UUID column, so no `@ManyToOne`/`UUID` typing. `status notification.notification_status NOT NULL DEFAULT 'PENDING'` and `priority notification.notification_priority NOT NULL DEFAULT 'NORMAL'` reuse `notification/enums/NotificationStatus`/`NotificationPriority` with `@Builder.Default` mirroring their SQL defaults. `subject`/`content TEXT` are nullable `String` with no `length`. `locale public.locale NOT NULL` (no default) reuses shared `global/enums/Locale`, same required-no-default treatment as `recipientType`. `scheduled_at TIMESTAMPTZ NOT NULL` (no default) is a required `Instant`; `sent_at` nullable `Instant`. `metadata JSONB` nullable `JsonNode`. Both `created_at`/`updated_at` at the end. PK is `notificationId`.
- **`NotificationQueueEntity`** (`notification.notification_queue`, `V56__notification_queue.sql`): single FK, `notification_id` → `@ManyToOne NotificationEntity notification` (`nullable = false`, `ON DELETE CASCADE`). `status notification.notification_status NOT NULL DEFAULT 'QUEUED'` reuses `NotificationStatus` with `@Builder.Default = NotificationStatus.QUEUED` (a different default member than `NotificationEntity.status`'s `PENDING` — mirror each table's own literal default, don't assume the same enum always defaults to the same constant). `available_at TIMESTAMPTZ NOT NULL` (no default) is a required `Instant`. `attempts INTEGER NOT NULL DEFAULT 0` → `@Builder.Default = 0`. `last_attempt_at` nullable `Instant`. Only `created_at` — no `updated_at` on this table, `@CreationTimestamp` + `updatable = false`. PK is `notificationQueueId`.
- **`NotificationDeliveryEntity`** (`notification.notification_deliveries`, `V57__notification_deliveries.sql`): single FK, `notification_id` → `@ManyToOne NotificationEntity notification` (`nullable = false`, `ON DELETE CASCADE`). `provider VARCHAR(100) NOT NULL` required `String` (`length = 100`); `provider_message_id VARCHAR(255)` nullable `String` (`length = 255`). `status notification.notification_status NOT NULL` has **no** SQL default here (unlike `NotificationEntity`/`NotificationQueueEntity`) — reuses `NotificationStatus` but **without** `@Builder.Default`, same required-but-no-default treatment as `NotificationTemplateEntity.locale`. `attempt INTEGER NOT NULL` (no default) required `Integer`. `error_message TEXT` nullable `String` with no `length`. `sent_at` nullable `Instant`. Only `created_at` — no `updated_at`, `@CreationTimestamp` + `updatable = false`. PK is `notificationDeliveryId`.
- **`NotificationPreferenceEntity`** (`notification.notification_preferences`, `V55__notification_preferences.sql`): single FK, `user_id` → `@ManyToOne UserEntity user` (`nullable = false`, `ON DELETE CASCADE`). `notification_type VARCHAR(50) NOT NULL` is a plain required `String`, **not** a native enum — same look-alike-but-actually-`VARCHAR` treatment as `ServiceEntity.periodType`. `email_enabled`/`sms_enabled`/`push_enabled`/`in_app_enabled BOOLEAN NOT NULL DEFAULT false` are all `@Builder.Default = false`. `quiet_hours_start`/`quiet_hours_end TIME` (nullable, no default) map to nullable `java.time.LocalTime` — first `TIME`-typed columns since `AvailabilityEntity.startTime`/`endTime` (those are required; these are optional). **First table in the codebase with only an `updated_at` column and no `created_at` at all** — `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` is mapped `@UpdateTimestamp` (not `@CreationTimestamp`), matching the column's actual name since there's no creation-timestamp counterpart to pair it with. Composite uniqueness `uq_notification_preferences_user_type` on `(user_id, notification_type)` is DB-level only, same treatment as `ServiceEntity.uq_services_organization_slug`. PK is `notificationPreferenceId`.
- **`notification` bounded context now fully implemented** — all six tables (`notification_channels`, `notification_templates`, `notifications`, `notification_preferences`, `notification_queue`, `notification_deliveries`) have entities.
- **`AuditActorEntity`** (`audit.audit_actors`, `V58__audit_actors.sql`) is the first `audit`-schema entity implemented, and the first table with **deliberately FK-less reference columns**: `user_uid`/`membership_id`/`widget_uid`/`api_key_uid`/`attendee_id` have no FK constraints at all in the migration (confirmed via the commented `database/init/audit/V100__audit_actors.sql` source — "Audit exists to be immutable historical evidence. A real FK with CASCADE would delete audit history when the referenced row is deleted, which defeats the point"), so all five stay plain columns (`String` for the `*_uid` ones, `BigInteger` for `membershipId`/`attendeeId`) with **no** `@ManyToOne`, regardless of whether `UserEntity`/`MembershipEntity`/`WidgetEntity`/`ApiKeyEntity` already exist — this is a permanent modeling decision, not the usual "upgrade once the entity exists" FK case. `user_uid`/`attendee_id`/`email` are nullable but `unique = true` (matching `uq_audit_actors_user_uid`/`uq_audit_actors_attendee_id`/`uq_audit_actors_email`). `actor_type audit.audit_actor_type NOT NULL` (no SQL default) reuses `audit/enums/AuditActorType` (native enum, values confirmed against `V3__create_enum_types.sql`) as `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)`, no `@Builder.Default` — required-but-no-default, same treatment as `BookingPolicyEntity.durationType`. `system_name VARCHAR(100)` nullable `String`; `display_name VARCHAR(255) NOT NULL` required `String`. `ip_address INET NOT NULL` is the codebase's first native Postgres `INET` column — mapped to `java.net.InetAddress` via `@JdbcTypeCode(SqlTypes.INET)` (confirmed against `hibernate-core` 7.4.1: `org.hibernate.type.descriptor.java.InetAddressJavaType` is the recommended Java type for `SqlTypes.INET`, paired with `PostgreSQLInetJdbcType`), same annotation family as the JSON/array/named-enum patterns — no extra dependency needed. `user_agent TEXT NOT NULL` required `String` with no `length`. Only `created_at` — no `updated_at` on this table, same shape as `auth.refresh_tokens`. PK is `auditActorId`.
- **`AuditResourceEntity`** (`audit.audit_resources`, `V60__audit_resources.sql`, index in `V72__indexes_audit.sql`): single FK in the migration, `event_id BIGINT NOT NULL` → `audit.audit_events(id)` `ON DELETE CASCADE` — originally left as a plain `BigInteger eventId` while `AuditEventEntity` was still an empty skeleton, then upgraded to `@ManyToOne AuditEventEntity event` (`nullable = false`) once `AuditEventEntity` was implemented, same precedent as `ApiKeyEntity.tenant`/`WidgetEntity.service`. `resource_type audit.resource_type NOT NULL` reuses `audit/enums/ResourceType` (native enum, values confirmed against `V3__create_enum_types.sql`) as `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)`, no `@Builder.Default` (required-but-no-default). `resource_uid`/`resource_name VARCHAR(255) NOT NULL` are required `String`. `before_snapshot JSONB` nullable `JsonNode`; `after_snapshot JSONB NOT NULL` required `JsonNode` — first table with one nullable and one required JSONB column side by side. Only `created_at` — no `updated_at`. PK is `auditResourceId`.
- **`AuditEventEntity`** (`audit.audit_events`, `V59__audit_events.sql`, indexes in `V72__indexes_audit.sql`) is the central `audit`-schema table — three FKs, all resolved to real `@ManyToOne` since every target entity already exists: `tenant_id` → `TenantEntity` (nullable, `ON DELETE SET NULL`), `organization_id` → `OrganizationEntity` (nullable, `ON DELETE SET NULL`), `actor_id` → `AuditActorEntity` (`nullable = false`, `ON DELETE CASCADE` — unlike the deliberately FK-less columns on `AuditActorEntity` itself, `audit_events.actor_id` **is** a real DB FK, so it gets a real relationship). `uid UUID NOT NULL DEFAULT gen_random_uuid()` (`unique = true`, matching `uq_audit_events_uid`) → `@UuidGenerator auditEventUid`, standard pattern. `correlation_id`/`request_id`/`trace_id VARCHAR(255)` are all nullable `String`. `event_domain audit.event_domain NOT NULL` reuses `audit/enums/AuditDomain` (native enum — Java class is named `AuditDomain`, not `EventDomain`, despite the column/Postgres-type being `event_domain`; values confirmed against `V3__create_enum_types.sql`). `event_type`/`action`/`severity`/`status` reuse `EventType`/`AuditAction`/`Severity`/`AuditStatus` respectively (all native enums, all `@Enumerated(STRING)` + `@JdbcTypeCode(NAMED_ENUM)`, none with `@Builder.Default` — required-but-no-default across the board). `context JSONB` is nullable `JsonNode`. `event_version INTEGER NOT NULL DEFAULT 1` → `@Builder.Default = 1`. This table has **two** separate `TIMESTAMPTZ DEFAULT now()` columns (`occurred_at`, `created_at`), both mapped `@CreationTimestamp` + `updatable = false` — Hibernate independently stamps `Instant.now()` into each at insert, so multiple `@CreationTimestamp` fields on one entity coexist without conflict; same reasoning as `WidgetEntity`'s three now()-columns, except this table has no `updated_at` counterpart. PK is `auditEventId`.
- **`AuditChangeEntity`** (`audit.audit_changes`, `V61__audit_changes.sql`, index in `V72__indexes_audit.sql`): single FK, `resource_id` → `@ManyToOne AuditResourceEntity resource` (`nullable = false`, `ON DELETE CASCADE`). `field_name VARCHAR(100) NOT NULL` required `String` with `length = 100`. `old_value`/`new_value TEXT` are both nullable `String` with no `length`. Only one timestamp column, named `occurred_at` rather than `created_at` (same non-`created_at`-named-but-still-`@CreationTimestamp` treatment as `CustomerActivityEntity.occurredAt`/`MembershipRoleEntity.assignedAt`) — `TIMESTAMPTZ NOT NULL DEFAULT now()`, no update counterpart, so `@CreationTimestamp` + `updatable = false`. PK is `auditChangeId`.
- **`audit` bounded context now fully implemented** — all four tables (`audit_actors`, `audit_events`, `audit_resources`, `audit_changes`) have entities.
- **Entity-level correction, caught only when building `AuditEvent` CRUD**: the note above for `AuditResourceEntity` had claimed `eventId` was already upgraded to `@ManyToOne AuditEventEntity event` once `AuditEventEntity` was implemented, but the actual `.java` file still had `eventId` as a plain `BigInteger` — the documentation was written ahead of the code change that was supposed to back it. Fixed by performing the actual upgrade (`@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "event_id", nullable = false) private AuditEventEntity event;`) while building `AuditEventRepository` in the CRUD pass below, since a real `AuditEventRepository` now exists to back the association. Lesson: when a doc note claims an upgrade happened, verify the actual entity file before relying on it, don't just trust the prose.
- **`AuditEventEntity` CRUD (`audit/event/`) — first `audit`-schema CRUD layer, and the first domain in the codebase with `update`/`delete` deliberately omitted for a business reason rather than a shape reason**: `docs/architecture/LOW_LEVEL_ARCHITECTURE.md`'s Audit Domain section states audit records must be "append-only"/"immutable" and "should never be updated or deleted through normal application workflows" — so `AuditEventService`/`AuditEventServiceImpl`/`AuditEventController` only implement `create`/`getById`/`getAll`, no `update`/`delete`. This extends the standing "omit a verb the entity's actual shape doesn't support" convention to also cover "omit a verb the domain's business policy forbids," even though `AuditEventEntity` does have several technically-mutable columns — the immutability constraint is a deliberate business rule, not a schema-shape observation, so don't reintroduce `update`/`delete` here even if a future column addition would otherwise make `update` look supportable. This same immutability reasoning should carry over to the remaining three `audit` tables (`audit_actors`, `audit_resources`, `audit_changes`) when their CRUD layers are built.
- Three FKs: `tenant_id`/`organization_id` (both nullable) resolve via the already-real `TenantRepository`/`OrganizationRepository` through a private `resolveTenant`/`resolveOrganization` helper each (returns `null` on a `null` request id, otherwise `findById().orElseThrow()`), same resolve-or-null pattern as `NotificationTemplateServiceImpl.resolveTenant`. `actor_id` (`nullable = false`) FKs into `AuditActorEntity` — originally resolved via a bare reference entity while `AuditActorRepository` was still a skeleton, then upgraded to a real `auditActorRepository.findById(request.actorId()).orElseThrow(AuditActorNotFoundException)` lookup once `AuditActorRepository` got its own CRUD pass (see below), same precedent as `TenantServiceImpl`'s `ecosystem_id`/`plan_id` upgrade once `EcosystemRepository`/`TenantPlanRepository` arrived. `V59__audit_events.sql` declares no unique constraint beyond `uq_audit_events_uid` (generated via `@UuidGenerator`, never client-supplied) — so, consistent with `Schedule`/`DestinationCalendar`/`Booking`/`Notification`/etc., only `AuditEventNotFoundException` exists, no `DuplicateAuditEventException`, and `AuditEventRepository` has no `existsBy*` method. `correlationId`/`requestId`/`traceId` (all nullable `VARCHAR(255)`, no default) get `@Size(max = 255)` only. `actorId`/`eventDomain`/`eventType`/`action`/`severity`/`status` (all `NOT NULL`, no SQL default) get `@NotNull`. `eventVersion` (`INTEGER NOT NULL DEFAULT 1`) is unannotated-but-guarded, same `@Builder.Default`-backed-optional pattern as everywhere else. `context` (JSONB, nullable) is excluded from both DTOs per the standing free-form-JSON-exclusion rule, same treatment as `metadata` elsewhere — `occurredAt`/`createdAt` (both `@CreationTimestamp`) are excluded from `AuditEventRequest` but included in `AuditEventResponse`. `AuditEventResponse.tenantId`/`organizationId` are nullable and null-guarded (`auditEvent.getTenant() != null ? ... : null`), same shape as `WidgetOriginResponse.createdById`/`NotificationTemplateResponse.tenantId`; `actorId` is unconditional since the association is `nullable = false`. Controller stays at `/api/v1/audit-events` with only `POST`/`GET /{auditEventId}`/`GET` mapped, no `PUT`/`DELETE`.
- **`AuditActorEntity` CRUD (`audit/actor/`)**: FK-free entity (per its own entity-level note, `user_uid`/`membership_id`/`widget_uid`/`api_key_uid`/`attendee_id` are deliberately plain scalar columns, never relationships) — so `create` needs no FK resolution at all, just a straight builder call off the request. `audit_actors` declares **three** separate single-column unique constraints (`uq_audit_actors_user_uid`, `uq_audit_actors_attendee_id`, `uq_audit_actors_email`), all on nullable columns — first domain in the codebase with a nullable-but-unique column. `AuditActorRepository` gets `existsByUserUid`/`existsByAttendeeId`/`existsByEmail`, and `createAuditActor` guards each check on the corresponding request field being non-null before calling it (`if (request.userUid() != null && auditActorRepository.existsByUserUid(...))`), consistent with Postgres unique-constraint semantics where multiple `NULL`s don't collide — throwing `DuplicateAuditActorException` on a hit. `actorType` (`NOT NULL`, no default) gets `@NotNull`; `displayName` (`VARCHAR(255) NOT NULL`) gets `@NotBlank @Size(max = 255)`; `userUid`/`widgetUid`/`apiKeyUid`/`email` (nullable `VARCHAR(255)`) get `@Size(max = 255)` only; `systemName` (nullable `VARCHAR(100)`) gets `@Size(max = 100)`; `membershipId`/`attendeeId` (nullable, no relationship) get no annotation; `ipAddress` (`INET NOT NULL`, non-`String` type) gets `@NotNull`; `userAgent` (`TEXT NOT NULL`, unbounded) gets `@NotBlank` only. Only `create`/`getById`/`getAll` — no `update`/`delete`, per the audit-immutability business rule established on `AuditEventEntity` above.
- **`AuditResourceEntity` CRUD (`audit/resource/`)**: single FK, `event_id` → the already-real `AuditEventRepository` (`AuditEventNotFoundException` on a miss). No unique constraint beyond its PK (confirmed against `V60__audit_resources.sql`), so only `AuditResourceNotFoundException` exists, no `Duplicate*Exception`. **`beforeSnapshot`/`afterSnapshot` (both JSONB) are deliberately kept in both DTOs, not excluded like a typical free-form `metadata` column** — unlike `AuditEventEntity.context` (optional contextual data, excluded) or `ServiceEntity.metadata`/`TenantEntity.settings` (generic extensibility blobs, excluded), the before/after snapshot pair **is** the core payload `audit_resources` exists to record; `afterSnapshot` is even `NOT NULL`, so excluding it would make the entity uncreatable via the API. `eventId` gets `@NotNull`; `resourceType` (native enum, `NOT NULL`, no default) gets `@NotNull`; `resourceUid`/`resourceName` (`VARCHAR(255) NOT NULL`) get `@NotBlank @Size(max = 255)`; `beforeSnapshot` (nullable `JsonNode`) gets no annotation; `afterSnapshot` (`JSONB NOT NULL`, non-`String` type) gets `@NotNull`. `AuditResourceResponse.eventId` is unconditional (`nullable = false` association). Only `create`/`getById`/`getAll` — no `update`/`delete`.
- **`AuditChangeEntity` CRUD (`audit/change/`)**: single FK, `resource_id` → the just-built `AuditResourceRepository` (`AuditResourceNotFoundException` on a miss). No unique constraint beyond its PK (confirmed against `V61__audit_changes.sql`), so only `AuditChangeNotFoundException` exists, no `Duplicate*Exception`. `resourceId` gets `@NotNull`; `fieldName` (`VARCHAR(100) NOT NULL`) gets `@NotBlank @Size(max = 100)`; `oldValue`/`newValue` (`TEXT`, nullable) get no annotation. Only `create`/`getById`/`getAll` — no `update`/`delete`.
- **`audit` bounded context CRUD layer now fully implemented** — all four `audit` tables (`audit_actors`, `audit_events`, `audit_resources`, `audit_changes`) have complete controller/service/repository/DTO/exception stacks, every one deliberately Create-Read-only (no `update`/`delete` anywhere in this bounded context) per the domain's append-only/immutable business policy documented in `docs/architecture/LOW_LEVEL_ARCHITECTURE.md`.

# Entity relationships

- **Many-to-one (owning FK side)**: `@ManyToOne(fetch = FetchType.LAZY)` + `@JoinColumn(name = "<fk_column>", nullable = ...)`, matching the migration's `NOT NULL`/nullability on the FK column. Examples: `AccountEntity.user` → `auth.accounts.user_id`; `ApiKeyEntity.tenant` → `auth.api_keys.tenant_id` (cross-bounded-context FK, `auth` entity pointing at `tenant`). DB-level `ON DELETE CASCADE` is not mirrored as a JPA cascade — cascade behavior lives in the DB, not the entity.
- **Many-to-many**: `@Builder.Default Set<...> = new HashSet<>()` + `@ManyToMany(fetch = FetchType.EAGER)` + `@JoinTable(joinColumns = ..., inverseJoinColumns = ...)`. Examples: `UserEntity.roles` (via `auth.user_roles`), `RoleEntity.permissions` (via `auth.role_permissions`).
  - **Inconsistency to watch**: `UserEntity`'s `@JoinTable` sets `schema = "auth"` explicitly; `RoleEntity`'s does not (relies on the default schema even though `role_permissions` lives in `auth`). Reconcile if it causes a resolution issue.

# Service & Controller conventions

Established with `UserController`/`UserService`/`UserServiceImpl` — the first controller, service, and DTO pair implemented in the codebase, so this is the reference pattern for every subdomain's CRUD layer going forward. `/Users/trungnguyen/Documents/Code/SoftwareEngineering/BusinessProjects/pleasebookme/CODING_CONVENTIONS.md` (repo root, one level up from `server/`) is the authoritative source for these rules — check it before adding a new controller/service/DTO, it isn't `@`-included from `CLAUDE.md`/`SERVER_AGENTS.md` so it's easy to miss.

- **Controller**: `@RestController` + `@RequestMapping("/api/v1/<plural-resource>")`, constructor injection via Lombok `@RequiredArgsConstructor` (not field `@Autowired`). Standard REST verbs: `POST` (create, `@ResponseStatus(HttpStatus.CREATED)`), `GET /{id}` (read one), `GET` (read all), `PUT /{id}` (update, full-replace semantics — the caller is expected to send the complete resource, not a partial patch), `DELETE /{id}` (`@ResponseStatus(HttpStatus.NO_CONTENT)`, no body). Controllers only translate between DTOs and the service layer — no entity references, no business logic.
  - **Known deviation from `CODING_CONVENTIONS.md`**: that file says controllers should "always return `ResponseEntity` with status number if possible," but `UserController` currently returns bare DTOs plus a class-level `@ResponseStatus` annotation instead. Not yet reconciled — only flagged here so the next controller (or a follow-up pass on `UserController`) picks the `ResponseEntity<T>` form when asked.
- **DTOs**: `CODING_CONVENTIONS.md` mandates records for every DTO. `dto/UserRequest` and `dto/UserResponse` are both plain `record`s (no Lombok on either — records get their own canonical constructor/accessors for free, so `@Getter`/`@Builder`/`@AllArgsConstructor` would be redundant). Record accessors follow record naming (`request.username()`, not `request.getUsername()`) — callers in the service layer use the bare accessor form. `UserResponse` keeps a static `from(UserEntity)` factory method (records can carry static methods same as any class) that maps entity → DTO field-by-field via the canonical constructor. Request/response DTOs only carry the subset of entity fields that make sense as external input/output — relationship collections (e.g. `UserEntity.roles`) and free-form `metadata` are deliberately left off both DTOs for now, not because they're unsupported, but because CRUD for those isn't in scope yet; add fields when a concrete need shows up rather than exposing everything preemptively.
- **Service interface + impl**: interface in `service/`, implementation in `service/impl/` as `<Name>ServiceImpl`, `@Service` + `@RequiredArgsConstructor` injecting the repository. Standard CRUD method set: `create<Entity>(RequestDto)`, `get<Entity>ById(id)` (plus any existing custom finders like `getUserByEmail`/`getUserByPhone`), `getAll<Entity>s()`, `update<Entity>(id, RequestDto)`, `delete<Entity>(id)`. `create`/`update` build/mutate the entity via its Lombok builder/setters directly from the request record's accessors — no separate mapper class.
- **Not-found/conflict handling**: superseded the original `ResponseStatusException`-only approach — a `@ControllerAdvice` now exists at `global/handler/GlobalExceptionHandler.java`, mapping typed exceptions to a shared `global/response/ApiErrorResponse.java` shape (`status`/`message`/`data`/`client`/`timestamp`/`path`). Per-subdomain exceptions live in that subdomain's own `exception/` package (e.g. `auth/user/exception/UserNotFoundException.java` + `DuplicateUserException.java`, `auth/password/exception/UserPasswordNotFoundException.java` + `DuplicateUserPasswordException.java`) — plain `RuntimeException` subclasses with a single `String message` constructor, no shared base class. Each gets its own `@ExceptionHandler` method in `GlobalExceptionHandler`: `*NotFoundException` → `HttpStatus.NOT_FOUND`, `Duplicate*Exception` → `HttpStatus.CONFLICT`. `global/exception/ResourceNotFoundException.java` still exists as a generic fallback for subdomains without their own typed exception yet. New subdomains should follow this typed-exception-per-domain pattern rather than throwing `ResponseStatusException` directly from the service layer.
- **Duplicate-check-before-create**: `create<Entity>` methods check uniqueness via repository `existsBy*`/`existsById` calls *before* building the entity, throwing the domain's `Duplicate*Exception` on a hit — established on `UserServiceImpl.createUser` (`existsByUsername`/`existsByEmail`/`existsByPhone`) and carried into `UserPasswordServiceImpl.createUserPassword` (`existsById`, since `UserPasswordEntity`'s PK **is** the FK). This matters most for shared-PK/`@MapsId` entities: `JpaRepository.save()` on an entity with a non-null `@Id` does a `merge` (silent UPDATE), not an INSERT-or-fail, so without the `existsById` guard a "create" call would silently overwrite an existing row instead of erroring.
- **CRUD for a shared-PK (`@MapsId`) entity**: `UserPasswordServiceImpl` is the first CRUD service built over a derived-identity entity (see `UserPasswordEntity`'s shared-PK note above). To create one, fetch the *parent* entity via its own repository (`UserRepository.findById(request.userId())`, throwing the parent's own `UserNotFoundException` if missing) and set only the association (`.user(user)`) on the builder — **not** the raw `@Id` field (`userUid`) directly; Hibernate derives it from the association via `@MapsId` at flush time. `update`/`delete` key off `userUid` exactly like `User`'s CRUD keys off its own PK, since here the shared PK doubles as the natural identifier.
  - **No "get all" endpoint**: unlike `User`, `UserPasswordController`/`UserPasswordService`/`UserPasswordServiceImpl` deliberately have **no** `getUserPasswords()`/`getAllUserPasswords()` — a bulk-list endpoint over a credentials table (even with `raw` since dropped from `PasswordResponse`) was removed on request rather than kept for CRUD-completeness symmetry with `User`. Don't re-add it by default when extending this controller; treat per-user lookup (`GET /{userId}`) as the only supported read shape here.
  - **`PasswordResponse` excludes `raw`**: unlike the original implementation (which mirrored every entity column, `UserResponse`-style), `PasswordResponse` was edited to drop the plaintext `raw` field from the API response — it still exists on `PasswordRequest`/`UserPasswordEntity` for the [[db_raw_password_field]]-documented onboarding-phase storage reason, but the response DTO no longer echoes it back over the wire.
  - **Password-reuse check on update**: `updateUserPassword` rejects a request whose `raw` equals the currently stored `raw`, throwing `DuplicateUserPasswordException` (→ 409, same handler as the create-time duplicate check). The comparison is a plain `String.equals` on plaintext — consistent with [[db_raw_password_field]]'s standing decision that `raw` is intentionally stored unhashed during the private-onboarding phase, not something to route through a hashing/verification layer that doesn't exist yet.
- **Bean validation**: `spring-boot-starter-validation` was added to `build.gradle.kts` once `UserRequest` needed constraint annotations, so `jakarta.validation.constraints.*` and `@Valid` are now available/wired up project-wide. `UserRequest` maps its `jakarta.validation` annotations directly off `V10__auth_users.sql`'s column definitions — `@NotBlank` on every `NOT NULL`-with-no-SQL-default `String` column (`username`, `name`, `email`, `phone`), `@Size(max = n)` mirroring each `VARCHAR(n)` length (`username` 100, `name`/`email` 255, `phone` 50, `timezone` 100), and no annotation on `bio`/`avatarUrl` (`TEXT`, nullable, no length limit). Columns with a SQL `DEFAULT` (`locale`, `timezone`, `theme`, `weekStart`) stay unannotated for nullability (no `@NotNull`) since the service layer already treats a null request value as "use the DB/entity default" (see `UserServiceImpl`'s `if (request.x() != null)` guards) — only `timezone`'s `@Size` applies when a value is actually supplied. Deliberately no `@Email` on `email` — the migration only declares `VARCHAR(255) NOT NULL`, no format constraint, and the ask was to mirror the migration exactly, not add extra validation semantics. For validation to actually trigger, `UserController`'s `createUser`/`updateUser` `@RequestBody UserRequest` parameters both need `@Valid` — added alongside the DTO annotations, otherwise the constraints are inert. A failed `@Valid` currently surfaces as Spring's default `MethodArgumentNotValidException` 400 response (no custom `@ExceptionHandler` shaping it yet, consistent with the "no global `@ControllerAdvice`" note above).
- **CRUD for pure composite-PK join tables (no surrogate `id`, no `@MapsId`)**: `UserRoleServiceImpl`/`RolePermissionServiceImpl` are the first CRUD services built directly over `UserRoleEntity`/`RolePermissionEntity` (both `@EmbeddedId`-keyed join tables, see their entity notes above). Repository is `JpaRepository<Entity, EmbeddedIdClass>` (e.g. `JpaRepository<UserRoleEntity, UserRoleId>`), so `findById`/`existsById`/`delete` all take a constructed `new UserRoleId(userId, roleId)` rather than a single scalar — no custom finder methods needed. `create` resolves *both* sides via their own repositories (`UserRepository`/`RoleRepository`, throwing each side's own `*NotFoundException`) and builds via `.user(user).role(role)` — same "set the association, not the raw FK field" pattern as the `UserPassword` `@MapsId` precedent — plus an `existsById` duplicate-check before create (`DuplicateUserRoleException`/`DuplicateRolePermissionException`, → 409). Controllers expose the composite key as two path segments: `GET/DELETE /api/v1/user-roles/{userId}/{roleId}`. **Deliberately no `update` endpoint/service method on either domain** — a join table with nothing but the composite key (`RolePermissionEntity`) or the key plus an immutable `@CreationTimestamp` (`UserRoleEntity.assignedAt`) has no mutable, non-key attribute to update; the only meaningful mutations are create (assign) and delete (revoke). Don't add a no-op `update` here by default — this mirrors the `UserPassword` precedent of omitting a CRUD verb that doesn't semantically fit the entity's actual shape (there: no bulk-list; here: no update).
- **Excluding credential/secret fields from response DTOs**: extending the `PasswordResponse`-excludes-`raw` precedent, `AccountResponse` (`auth/account/`) drops `accessToken`/`refreshToken`/`idToken` (live OAuth credentials) and `RefreshTokenResponse` (`auth/refreshtoken/`) drops `secret` (a bearer credential) even though `AccountRequest`/`RefreshTokenRequest` and the entities still carry them (needed to persist). `ApiKeyResponse` (`auth/apikey/`) keeps `publicKey` (a public identifier, safe to echo back) but drops `secretHash`. None of these were explicitly requested per-field — treat "don't echo a stored secret/credential back over the API" as a standing default for any future `*Response` DTO wrapping a token/credential-bearing entity, not just a one-off.
- **Building a "reference" entity for an FK into a domain with no repository/service yet**: `ApiKeyEntity.tenant` (→ `tenant.tenants`) and `RefreshTokenEntity.widget` (→ `widget.widgets`) are real `@ManyToOne` associations (per the FK-upgrade precedent above), but neither `TenantRepository` nor `WidgetRepository` exists yet — only their entities do. `ApiKeyServiceImpl`/`RefreshTokenServiceImpl` construct a bare reference instance instead (e.g. `TenantEntity.builder().tenantId(request.tenantId()).build()`, `WidgetEntity.builder().widgetId(request.widgetId()).build()`) and set that on the association — Hibernate only needs the associated entity's `@Id` populated to write the FK column, it doesn't need the full row loaded. **Trade-off to know**: this skips existence validation (no `findById`/`orElseThrow`), so an invalid `tenantId`/`widgetId` surfaces as a raw DB-level FK-violation 500, not a clean 404 — same category of gap as the pre-`@Valid` era. Once `TenantRepository`/`WidgetRepository` exist, replace this with a real `findById(...).orElseThrow(new TenantNotFoundException(...))` lookup, the same as every other cross-domain FK in this codebase.
- **`ServiceEntity` CRUD (`core/service/`) — first `core`-domain CRUD layer**: all six FKs are now resolved via real repository lookups. `user_id` → `UserRepository`. `profile_id`/`organization_id` → `ProfileRepository`/`OrganizationRepository`, upgraded from bare reference entities once `Profile`/`Organization` CRUD was built (see their own bullets below). `schedule_id` → `ScheduleRepository`, and the two nullable FKs `destination_calendar_id`/`destination_sheets_id` → `DestinationCalendarRepository`/`DestinationSheetsRepository` (only looked up when the request supplies a non-null id; `update` explicitly nulls the association out via an `else` branch when the request field is null, since these columns have no default to "preserve" and full-replace semantics apply) — all three upgraded from reference entities in the same pass that built out `Schedule`/`DestinationCalendar`/`DestinationSheets` CRUD (see their own bullets below). `BusinessServiceImpl` (the `ServiceEntity` one, in `core/service/`) now injects seven repositories total: `ServiceRepository`, `UserRepository`, `ProfileRepository`, `OrganizationRepository`, `ScheduleRepository`, `DestinationCalendarRepository`, `DestinationSheetsRepository`. No more reference-entity workarounds remain on this entity.
  - **Composite-unique duplicate check, contra the entity-level note**: `ServiceEntity`'s own doc comment above says `uq_services_organization_slug` is "DB-level only, not enforced at the entity level" — that statement is about JPA/Hibernate-level annotations (no `@Column(unique = true)` equivalent for a composite key), not about service-layer business logic. `CODING_CONVENTIONS.md` still mandates a duplicate-check for every unique constraint a table declares, so `ServiceRepository` gets `existsByOrganizationOrganizationIdAndSlug(organizationId, slug)` (Spring Data nested-property traversal through the `organization` association into its `organizationId` field) and `BusinessServiceImpl.createService` calls it before building the entity, throwing `DuplicateServiceException` on a hit.
  - **Interface/impl were later renamed `BusinessServiceService`/`BusinessServiceImpl`**, superseding the original `OrganizationServiceService`/`OrganizationServiceImpl` naming — these were pre-created as skeleton files with the `OrganizationService*` naming (disambiguating the "a business's bookable service" domain concept from the generic Spring `@Service` term) before this CRUD pass began, but that name still collided with `organization/organizations/service/impl/OrganizationServiceImpl.java` (see the `OrganizationEntity` CRUD note below), so both the interface and impl were renamed to `BusinessServiceService`/`BusinessServiceImpl` to remove the collision entirely rather than rely on package-path vigilance. `ServiceController`'s injected field is named `businessService` accordingly.
  - **DTOs exclude `metadata`** (JSONB, per the standing "don't expose free-form JSON preemptively" rule) but include every other scalar/FK column; `ServiceRequest` has no presence annotation on `interfaceLanguage`/`periodType`/`timezone`/`currency`/the four `Boolean` flags since all six have a SQL `DEFAULT` mirrored by `@Builder.Default` on the entity — `periodType`/`timezone` still keep `@Size(max = 50)`/`@Size(max = 100)` respectively (bounded `VARCHAR`, validated only when supplied), matching the `UserRequest.timezone` precedent. `title`/`slug` get `@NotBlank @Size(max = 255)`; `userUid`/`profileId`/`organizationId`/`scheduleId` get `@NotNull`; `description`/`location`/`successRedirectUrl` (all `TEXT`, nullable) and `minPrice`/`maxPrice`/`maxActiveBookingPerBooker`/`destinationCalendarId`/`destinationSheetsId` (all nullable, no default) get no annotation at all.
- **`OrganizationEntity` CRUD (`organization/organizations/`)** — built to unblock `ServiceEntity`'s `organization_id` FK (see above), first `organization`-schema CRUD layer. `OrganizationEntity` is a root FK-free entity, so `OrganizationServiceImpl.createOrganization` has no association to resolve — only the `existsBySlug` duplicate-check (backing `uq_organizations_slug`) before building. `isPrivate`/`timezone`/`weekStart` all have `@Builder.Default`-mirrored SQL defaults, so `OrganizationRequest` has no presence annotation on any of the three (`timezone` still keeps `@Size(max = 100)`) and `create`/`update` guard each with `if (request.x() != null)`, same pattern as `ServiceRequest`. `logoUrl`/`bannerUrl`/`bio` are nullable `TEXT`, no annotation. `metadata` excluded from both DTOs. `OrganizationResponse` carries every scalar column plus both timestamps — no `uid` field on this entity (root organizations don't have one, unlike `ProfileEntity`).
  - **Name collision, since resolved by renaming the other one**: this originally created a *second* class named `OrganizationServiceImpl` in the codebase — the pre-existing one at `core/service/service/impl/OrganizationServiceImpl.java` implemented `OrganizationServiceService` (CRUD for a business's bookable *Service*), while this one at `organization/organizations/service/impl/OrganizationServiceImpl.java` implements `OrganizationService` (CRUD for the *Organization* entity itself). Both compiled fine since they're different packages, but the collision was later removed by renaming the `core/service/` pair to `BusinessServiceService`/`BusinessServiceImpl` (see the `ServiceEntity` CRUD note above) — `organization/organizations/service/impl/OrganizationServiceImpl.java` keeps its original name, it's the only `OrganizationServiceImpl` in the codebase now.
- **`ProfileEntity` CRUD (`organization/profile/`)** — the other FK `ServiceEntity` depends on, built alongside `OrganizationEntity` CRUD in the same pass so `ProfileRepository` exists too. Two FKs, both resolved via real repositories from the start (`UserRepository` already existed; `OrganizationRepository` was implemented immediately before this in the same request): `userUid` → `UserRepository.findById().orElseThrow(UserNotFoundException)`, `organizationId` → `OrganizationRepository.findById().orElseThrow(OrganizationNotFoundException)`. `ProfileEntity` has **two separate composite-unique constraints** on the same table (`uq_profiles_user_organization` on `(user_id, organization_id)` and `uq_profiles_username_organization` on `(username, organization_id)`) — both get their own `existsBy*` method on `ProfileRepository` (`existsByUserUserIdAndOrganizationOrganizationId`, `existsByUsernameAndOrganizationOrganizationId`, both using Spring Data's nested-property traversal through the `user`/`organization` associations into their PK fields) and both are checked in `createProfile` before either FK is resolved, each throwing `DuplicateProfileException` on a hit. The third unique constraint on this table, `uq_profiles_uid`, is **not** given an `existsBy` check — `uid` is Hibernate-generated via `@UuidGenerator` on create, never client-supplied, so there's nothing for a client request to collide on. `ProfileRequest` only carries `userUid`/`organizationId` (`@NotNull`) and `username` (`@NotBlank @Size(max = 100)`) — the entity's only plain scalar column. `ProfileResponse` includes `profileUid` alongside `profileId` (first `Response` in this domain pass with a UID field, unlike `OrganizationResponse`).
- **`ScheduleEntity` CRUD (`core/schedule/`)**, **`DestinationCalendarEntity` CRUD (`integration/calendar/`)**, and **`DestinationSheetsEntity` CRUD (`integration/sheets/`)** — built together to close out the last three reference-entity workarounds on `ServiceEntity` (see above). `ScheduleEntity` has a single FK (`user_id` → `UserRepository`); `DestinationCalendarEntity`/`DestinationSheetsEntity` each have two (`user_id` → `UserRepository`, `service_id` → `ServiceRepository`, already implemented). All three tables' `create`/`update` resolve every FK via real `findById().orElseThrow()` — no reference entities anywhere in this batch, since every target repository already existed by the time these were built.
  - **No `Duplicate*Exception` on any of these three domains** — first deliberate extension of the "omit a verb/behavior the entity's actual shape doesn't support" precedent (previously applied only to CRUD verbs like `getAll`/`update`, see the `UserPassword`/`UserRole` notes above) to the *exception* layer itself. `core.schedules`, `integration.destination_calendars`, and `integration.destination_sheets` each declare **no unique constraint beyond their surrogate PK** (confirmed against `V26__core_schedules.sql`, `V49__destination_calendars.sql`, `V50__destination_sheets.sql`, and their respective index migrations — plain non-unique `CREATE INDEX`, not `UNIQUE`). `CODING_CONVENTIONS.md`'s "every domain gets exactly two typed exceptions" is read as the default case, not an unconditional rule — registering a `Duplicate*Exception`/`@ExceptionHandler` pair that can never actually be thrown (no `existsBy*` call anywhere to trigger it) is exactly the kind of dead, half-finished code the project's engineering guidance says to avoid. So each of these three domains gets only a `*NotFoundException`, and their repositories have no `existsBy*` methods at all. If a future migration adds a unique constraint to any of these three tables, add the matching `Duplicate*Exception` + `existsBy*` check at that point, not before.
  - **`ScheduleRequest`**: `userUid` `@NotNull`; `title` (`VARCHAR(255) NOT NULL`, no default) `@NotBlank @Size(max = 255)`; `timezone` (`VARCHAR(100) NOT NULL DEFAULT 'Australia/Sydney'`) no presence annotation but keeps `@Size(max = 100)`, same `@Builder.Default`-guarded-optional pattern as `ServiceRequest.timezone`/`OrganizationRequest.timezone`.
  - **`DestinationCalendarRequest`/`DestinationSheetsRequest`** (identical shape, mirroring their identical entity columns): `integrationType` (`NOT NULL`, no default, native enum) `@NotNull`; `externalId` (`VARCHAR(255) NOT NULL`, no default) `@NotBlank @Size(max = 255)`; `userUid`/`serviceId` both `@NotNull`. Responses flatten both FKs to raw ids and carry every scalar column plus both timestamps — no `metadata`/`uid` columns on either table.
- **`AvailabilityEntity` CRUD (`core/availability/`)**: two FKs (`user_id` → `UserRepository`, `schedule_id` → `ScheduleRepository`), both resolved via real lookups since both repositories already existed. `days` (`INTEGER[] NOT NULL`, the array column) maps to `@NotNull Integer[] days` on the request — treated as a non-`String` required type per the validation table (`@NotNull`, not `@NotBlank`/`@Size`). `startTime`/`endTime` (`TIME NOT NULL`, no default) are `@NotNull LocalTime`. No unique constraint on `core.availabilities` beyond its PK (confirmed against `V27__core_availabilities.sql` + `V67__indexes_core.sql`'s plain `CREATE INDEX idx_availabilities_user_id`/`idx_availabilities_schedule_id`) — same "omit `Duplicate*Exception`" treatment as `Schedule`/`DestinationCalendar`/`DestinationSheets` above, only `AvailabilityNotFoundException` exists.
- **`BookingPolicyEntity` CRUD (`core/bookingpolicy/`)**: single FK, `service_id` → `ServiceRepository` (already exists). No unique constraint on `core.booking_policies` (confirmed against `V29__core_booking_policies.sql`, which has no index migration entry at all) — despite `service_id` intuitively feeling like it should be one-policy-per-service, the migration doesn't declare that constraint, so per the standing "don't invent constraints the migration doesn't declare" rule, no `existsBy*`/`DuplicateBookingPolicyException` was added; only `BookingPolicyNotFoundException`. Nine fields have a SQL `DEFAULT` (`bookingMode`, `defaultDuration`, `slotInterval`, `beforeBuffer`, `afterBuffer`, `allowOverlap`, `allowMultipleAttendee`, `requiresPayment`, `autoConfirm`) and all nine get the `@Builder.Default`-guarded-optional treatment (no presence annotation, `if (request.x() != null)` in `create`/`update`) — the largest number of optional/defaulted fields on one `Request` DTO so far. `durationType`/`bookingWindowType` (`VARCHAR(50) NOT NULL`, no default) get `@NotBlank @Size(max = 50)`; `minimumNotice`/`maximumAdvanceBooking`/`capacity` (`INTEGER NOT NULL`, no default) get `@NotNull`; `minimumDuration`/`maximumDuration` are nullable `Integer`, no annotation.
- **`BookingEntity` CRUD (`core/booking/`)** — the busiest FK table in `core`, and the entity every other domain in this batch ultimately depends on (`Attendee` FKs into it). Two required FKs (`user_id` → `UserRepository`, `service_id` → `ServiceRepository`) resolved unconditionally; three **nullable** self-referencing FKs into `auth.users` (`cancelled_by`, `rescheduled_by`, `deleted_by`) are resolved through a shared private `resolveUser(BigInteger)` helper on `BookingServiceImpl` that returns `null` when the request's id field is `null` and otherwise does a real `findById().orElseThrow(UserNotFoundException)` — first helper-method extraction in the codebase for a repeated "nullable FK, resolve-or-null" pattern (three call sites would otherwise triplicate the same four lines). The two nullable `destination_calendar_id`/`destination_sheets_id` FKs follow the same non-null-guarded real-lookup pattern already established on `ServiceEntity`'s own `BusinessServiceImpl`. `uq_bookings_uid` is **not** given an `existsBy` check — `uid` is `@UuidGenerator`-generated, never client-supplied, same treatment as every other generated-`uid` table in the codebase — so only `BookingNotFoundException` exists, no `DuplicateBookingException`. `idempotencyKey` (nullable `VARCHAR(255)`) gets `@Size(max = 255)` only, despite its name suggesting a dedup key — the migration declares no `UNIQUE` constraint on it, so (per the same "don't invent constraints" rule as `BookingPolicy` above) it's treated as a plain optional string, not a duplicate-check field. `deletedAt`/`deletedById` are included as plain optional `Request` fields (not excluded) since the entity's `deleted_at` column is a plain nullable `Instant`, **not** a `@CreationTimestamp`/`@UpdateTimestamp`-annotated column — the DTO field-exclusion rule only excludes PK/UID/timestamp-annotated columns, and soft-delete state doesn't qualify, so it's exposed like any other nullable column under full-replace CRUD semantics (a dedicated cancel/delete workflow, if ever built, would be a separate concern layered on top, not a reason to hide the column from CRUD now).
  - **Pre-existing skeleton bug fixed**: `BookingServiceImpl`'s stub (`class BookingServiceImpl implements AvailabilityService`) was a copy-paste error — implemented `AvailabilityService` instead of `BookingService`. Corrected as part of this implementation pass, same category of fix as the earlier `RefreshTokenEntity`/`@Table(name = "api_keys")` copy-paste bug — always verify a pre-existing skeleton's `implements`/`@Table` target actually matches its own domain before building on top of it.
- **`AttendeeEntity` CRUD (`core/attendee/`)**: single FK, `booking_id` → `BookingRepository` (built immediately before this in the same batch, since `Attendee` depends on it). Only `created_at` on this entity (no `updated_at`), but unlike the pure-join-table "no update verb" precedent (`UserRole`/`RolePermission`), `AttendeeEntity` has several real mutable business columns (`email`, `phone`, `name`, `locale`, `timezone`, `noShow`) on a surrogate PK, so the standard `update` verb **is** included — the "omit a verb the entity's shape doesn't support" rule is about the entity having nothing mutable to update, not about whether it happens to lack an `updated_at` timestamp column. `locale` (nullable, no default) carries no annotation, same treatment as the identical column on `CustomerEntity`/the original `AttendeeEntity` note. No unique constraint on `core.attendees` beyond its PK — `AttendeeNotFoundException` only.
- **`SelectedSlotEntity` CRUD (`core/selectedslot/`)** — the only domain in this batch with a real, client-supplied composite-unique constraint. Two FKs (`service_id` → `ServiceRepository`, `user_id` → `UserRepository`), both real lookups. `uq_selected_slots_slot` on `(service_id, user_id, slot_start, slot_end)` backs `existsByServiceServiceIdAndUserUserIdAndSlotStartAndSlotEnd` on `SelectedSlotRepository` (four-property nested-path traversal — two through associations into their PK, two direct scalar columns), checked in `createSelectedSlot` before any FK resolution, throwing `DuplicateSelectedSlotException` on a hit — same treatment CODING_CONVENTIONS.md mandates, consistent with the `ServiceEntity`/`ProfileEntity` precedents, and the one place in this batch where the earlier `uq_selected_slots_slot`-is-"DB-level-only" entity note does **not** extend to skipping the service-layer duplicate check (that note is about JPA/Hibernate column annotations, not `create()` business logic — same distinction already drawn for `ServiceEntity`). `uq_selected_slots_uid` gets no check (generated, not client-supplied). `update` does **not** re-check the composite constraint — consistent with `ServiceEntity.updateService`, which likewise never re-validates `uq_services_organization_slug` on update; the standard convention's duplicate-check step is scoped to `create` only.
- **`OutOfOfficeEntity` CRUD (`core/outofoffice/`)**: two FKs, both into `auth.users` (`user_id` → `user`, `to_user_id` → `toUser`), both required real lookups via `UserRepository`. `reason` (`TEXT NOT NULL`, no default, unbounded) gets `@NotBlank` only, no `@Size` — first `TEXT`-typed required field in this batch (`BookingPolicy`/`Booking`'s required strings were all bounded `VARCHAR`). `uq_out_of_office_uid` is generated, not client-supplied, so no duplicate check — `OutOfOfficeNotFoundException` only. Controller stays at its pre-existing skeleton mapping `/api/v1/ooo` (not `/api/v1/out-of-office`) since that `@RequestMapping` was already set before this pass and wasn't asked to change.
- **`core` bounded context CRUD layer now fully implemented** — all eight `core` tables (`schedules`, `availabilities`, `services`, `booking_policies`, `bookings`, `attendees`, `selected_slots`, `out_of_office`) have complete controller/service/repository/DTO/exception stacks, not just entities. `ServiceEntity`'s `BusinessServiceImpl` needed no further changes in this pass — none of these six entities are FKs *on* `ServiceEntity` (they all FK *into* it, the reverse direction), so the "update Service if needed" check from this task came back negative.
- **`TenantEntity` CRUD (`tenant/tenants/`)** and **`WidgetEntity` CRUD (`widget/widgets/`)** — built together in one pass specifically because `WidgetEntity.tenant` FKs into `TenantEntity`, so `TenantRepository` had to exist first before `WidgetServiceImpl` could resolve it as a real lookup rather than a reference entity. `uq_tenants_slug` backs `TenantRepository.existsBySlug` (`DuplicateTenantException` on a hit); `uq_widgets_public_key` backs `WidgetRepository.existsByPublicKey` (`DuplicateWidgetException`). Both `uq_tenants_uid`/`uq_widgets_uid` are generated, not client-supplied, so neither gets a check.
  - **`TenantEntity`'s four FKs split two ways depending on whether the target's repository is a real `JpaRepository` yet**: `organization_id` → `OrganizationRepository` and `owner_user_id` → `UserRepository` are both real repositories already, so they resolve via `findById().orElseThrow()` (`OrganizationNotFoundException`/`UserNotFoundException`, both pre-existing). `ecosystem_id` and `plan_id` FK into `tenant.ecosystems`/`tenant.plans`, but `EcosystemRepository`/`TenantPlanRepository` are still bare skeleton interfaces (`public interface EcosystemRepository { }`, not extending `JpaRepository`) even though `EcosystemEntity`/`TenantPlanEntity` themselves are fully implemented — so both fall back to the reference-entity pattern (`EcosystemEntity.builder().ecosystemId(request.ecosystemId()).build()` / `TenantPlanEntity.builder().tenantPlanId(request.planId()).build()`), same precedent as the original `ApiKeyEntity.tenant`/`WidgetEntity.service` bare-reference era. This confirms the rule is keyed off "does a real repository exist," not "does the entity class exist" — `TenantPlanEntity`/`EcosystemEntity` have existed since an earlier pass, but their CRUD layers (repository/service/controller) were never built, so the FK still can't do a real existence check yet. Revisit both once `EcosystemRepository`/`TenantPlanRepository` get their own CRUD pass.
  - **`WidgetEntity`'s two FKs are both real lookups from the start**: `tenant_id` → the just-built `TenantRepository` (`TenantNotFoundException`), `service_id` → the pre-existing `ServiceRepository` (`ServiceNotFoundException`). No reference-entity workaround anywhere in `WidgetServiceImpl`.
  - **`WidgetResponse` excludes `secretKey`**: extending the `PasswordResponse`/`AccountResponse`/`RefreshTokenResponse` precedent for never echoing a live credential back over the API — `widget.secret_key` is a bearer credential (the widget's private signing/auth secret), so it stays on `WidgetRequest`/`WidgetEntity` (needed to persist it) but is dropped from `WidgetResponse`. `publicKey` is kept in the response, same as `ApiKeyResponse.publicKey` — a public identifier is safe to round-trip, a secret is not.
  - **`WidgetEntity.issuedAt`** (`@CreationTimestamp`, no `updatable = false` — see its original entity note) is excluded from `WidgetRequest` per the standard "any `@CreationTimestamp`/`@UpdateTimestamp` column is excluded from Request" rule, but **is** included in `WidgetResponse` alongside `createdAt`/`updatedAt` — it's business-meaningful data (when the widget's credentials were actually issued, distinct from the audit-only `created_at`), not just bookkeeping, so it's treated as a plain scalar column for response purposes rather than folded into the "both timestamps" audit pair.
  - **`WidgetEntity.lastUsedAt`** (nullable `Instant`, no default, no timestamp annotation) is a plain optional column on both `WidgetRequest`/`WidgetResponse` — same "not annotated `@CreationTimestamp`/`@UpdateTimestamp` means it's just a regular nullable field under full-replace semantics" treatment as `BookingEntity.deletedAt`, even though a `lastUsedAt` field reads like something only the server should stamp. No dedicated "mark as used" endpoint was introduced — that would be a separate concern layered on top of CRUD, not a reason to hide the column from the standard `Request`/`Response` shape now.
  - **`TenantRequest`/`WidgetRequest` validation follows the migration exactly**: `TenantEntity`'s `default_timezone`/`default_locale` (both `NOT NULL DEFAULT`) get no presence annotation (guarded `if (request.x() != null)` in `create`/`update`, `defaultTimezone` keeps `@Size(max = 100)`); `status`/`region`/the three `max_*` Integer columns (`NOT NULL`, no default) all get `@NotNull`. `WidgetEntity`'s `status`/`type`/`originValidation` (all `NOT NULL DEFAULT`) are likewise unannotated-but-guarded; `publicKey`/`secretKey` are `TEXT NOT NULL` (unbounded) so `@NotBlank` only, no `@Size`, same treatment as `OutOfOfficeEntity.reason`. `settings` (`TenantEntity`, JSONB) is excluded from both DTOs per the standing metadata-exclusion rule.
- **`EcosystemEntity` CRUD (`tenant/ecosystem/`)** and **`TenantPlanEntity` CRUD (`tenant/plan/`)** — both FK-free lookup tables (same shape as `OrganizationEntity`), built together specifically to close out `TenantServiceImpl`'s two remaining reference-entity workarounds (see the `TenantEntity` CRUD note above). `uq_tenant_ecosystems_code`/`uq_tenant_plans_code` back `existsByCode` on each repository, throwing `DuplicateEcosystemException`/`DuplicateTenantPlanException`. `EcosystemEntity.status` (`NOT NULL DEFAULT 'REVIEWING'`) and `TenantPlanEntity.currency` (`NOT NULL DEFAULT 'USD'`) are both unannotated-but-guarded in their `Request`s, same as every other `@Builder.Default`-backed column; every other required scalar column (`code`/`name`/`icon`, `description` as unbounded `TEXT` → `@NotBlank` only, `price`, the five `max_*` Integers on `TenantPlanEntity`) gets `@NotNull`/`@NotBlank`(`+@Size`) straight off the migration. `metadata`/`features` (JSONB) excluded from both entities' DTOs.
  - **`TenantServiceImpl` upgraded in the same pass**: `ecosystem_id`/`plan_id` now resolve via real `EcosystemRepository.findById()`/`TenantPlanRepository.findById()` lookups (throwing the newly-added `EcosystemNotFoundException`/`TenantPlanNotFoundException`) instead of the bare `EcosystemEntity.builder().ecosystemId(...).build()`/`TenantPlanEntity.builder().tenantPlanId(...).build()` reference-entity workaround from the initial `TenantEntity` CRUD pass. This is the concrete playing-out of the "upgrade once a real repository exists" rule flagged as a to-do in that earlier note — confirms the trigger is specifically "does `<Target>Repository` extend `JpaRepository` yet," independent of whether the CRUD pass that added it also touched the FK-holding entity.
- **`TenantDomainEntity` CRUD (`tenant/domain/`)**: single FK, `tenant_id` → the already-real `TenantRepository` (`TenantNotFoundException`). `uq_tenant_domains_domain` backs `existsByDomain` (`DuplicateTenantDomainException`). `verified`/`isPrimary` (`BOOLEAN NOT NULL DEFAULT`) are unannotated-but-guarded; `domain` (`VARCHAR(255) NOT NULL`) gets `@NotBlank @Size(max = 255)`; `verificationToken` (`TEXT NOT NULL`, unbounded) gets `@NotBlank` only. **`verificationToken` is deliberately kept in `TenantDomainResponse`**, unlike every other token/secret/credential column excluded per the standing rule (`PasswordResponse.raw`, `WidgetResponse.secretKey`, etc.) — this token is a DNS-verification value the *client is expected to read back and publish* (e.g. as a DNS TXT record or meta tag) to prove domain ownership, not a bearer credential used to authenticate as the tenant. The credential-exclusion rule is about live authentication secrets; a value the API consumer needs to externally publish to complete a workflow doesn't qualify, so it stays in the response.
- **`WidgetOriginEntity` CRUD (`widget/widgetorigin/`)**: two FKs — `widget_id` → `WidgetRepository` (`nullable = false`, real lookup, `WidgetNotFoundException`), `created_by` → `UserRepository` but **nullable** (`ON DELETE SET NULL`), resolved through a private `resolveUser(BigInteger)` helper that returns `null` on a `null` id and otherwise does a real `findById().orElseThrow(UserNotFoundException)` — same resolve-or-null helper pattern established on `BookingServiceImpl` for its three nullable `auth.users` FKs. `uq_widget_origins_widget_origin` is a **composite** unique constraint on `(widget_id, origin)`, so it backs `existsByWidgetWidgetIdAndOrigin(widgetId, origin)` (nested-path traversal through the `widget` association into its PK, same shape as `ServiceRepository.existsByOrganizationOrganizationIdAndSlug`), throwing `DuplicateWidgetOriginException`. This table only has `created_at` (no `updated_at`), but — same reasoning as `AttendeeEntity` — it still has real mutable columns (`widget`, `origin`, `verified`, `createdBy`) on a surrogate PK, so the standard `update` verb is included rather than omitted. `WidgetOriginResponse.createdById` is nullable and flattened with a null-guard (`widgetOrigin.getCreatedBy() != null ? ... : null`) since the association itself can be null.
- **`tenant` and `widget` bounded contexts now fully implemented** — all four `tenant` tables (`tenants`, `ecosystems`, `plans`, `tenant_domains`) and both `widget` tables (`widgets`, `widget_origins`) have complete controller/service/repository/DTO/exception stacks.
- **`ResourceTypeEntity` CRUD (`resource/type/`)** and **`ResourceEntity` CRUD (`resource/resources/`)** — first two `resource`-schema CRUD layers, built together since `ResourceEntity.resource_type_id` FKs into `resource_types`. `ResourceTypeEntity` has a single FK (`organization_id` → `OrganizationRepository`, already real). `V34__resource_types.sql` declares no `UNIQUE` constraint beyond its surrogate PK, so — same "don't invent constraints the migration doesn't declare" treatment as `Schedule`/`DestinationCalendar`/`BookingPolicy`/`Availability`/`Attendee`/`OutOfOffice` above — `ResourceTypeRepository` gets no `existsBy*` method and only `ResourceTypeNotFoundException` exists, no `DuplicateResourceTypeException`. `description` (`TEXT`, nullable, no default) carries no validation annotation; `name`/`icon` (`VARCHAR(100) NOT NULL`, no default) get `@NotBlank @Size(max = 100)`.
  - **`ResourceEntity` has three FKs**, all real lookups from the start since every target repository already existed by the time this was built: `organization_id` → `OrganizationRepository`, `service_id` → `ServiceRepository`, `resource_type_id` → the just-built `ResourceTypeRepository`. `uq_resources_organization_slug` on `(organization_id, slug)` backs `existsByOrganizationOrganizationIdAndSlug` on `ResourceRepository` (identical shape to `ServiceRepository`'s own method), checked in `createResource` before any FK resolution, throwing `DuplicateResourceException` on a hit — `uq_resources_uid` gets no check since `uid` is `@UuidGenerator`-generated, never client-supplied, same treatment as every other generated-`uid` table. `description`/`capacity`/`status` (`TEXT NOT NULL`/`INTEGER NOT NULL`/`VARCHAR(50) NOT NULL`, none with a SQL default) get `@NotBlank`/`@NotNull`/`@NotBlank @Size(max = 50)` respectively — `status` stays a plain `String` per its entity-level "look-alike but actually `VARCHAR`" note, not a native enum. `isBookable`/`isVirtual` (`BOOLEAN NOT NULL DEFAULT`) are unannotated-but-guarded on `ResourceRequest`, same `@Builder.Default`-backed-optional pattern as everywhere else.
  - **Naming collision, deliberately allowed this time**: `resource.resources.exception.ResourceNotFoundException` (the new domain-specific one, named off `ResourceEntity` per the class-name-driven exception convention) has the same simple name as the pre-existing generic fallback `global.exception.ResourceNotFoundException`. Since a domain's typed exception is meant to *replace* the generic fallback for that domain (not coexist with a differently-named one), keeping the name `ResourceNotFoundException` is the correct application of the convention, not an oversight — unlike the `OrganizationServiceImpl` collision above (which got renamed away because both classes were equally "real" domain services), this collision is expected and permanent. `GlobalExceptionHandler` cannot import both under the same simple name, so the domain-specific one is referenced by its fully-qualified name in the `@ExceptionHandler`/parameter type instead of an import; the generic one keeps its existing import and handler untouched.
  - **Both controllers map to their own top-level plural paths**: `/api/v1/resource-types` and `/api/v1/resources` (both pre-existing skeleton `@RequestMapping`s, unchanged).
- **`ResourcePricingEntity` CRUD (`resource/pricing/`)**, **`ResourceAssignmentEntity` CRUD (`resource/assignment/`)**, **`ResourceCalendarEntity` CRUD (`resource/calendar/`)**, **`ResourceMaintenanceEntity` CRUD (`resource/maintenance/`)**, **`ResourceOverrideEntity` CRUD (`resource/overrides/`)**, and **`ResourceAttributesEntity` CRUD (`resource/attribute/`)** — the remaining six `resource`-schema CRUD layers, closing out the bounded context. Every one of `V36`–`V41`/`V74` declares **no `UNIQUE` constraint beyond its own PK**, so — same "don't invent constraints the migration doesn't declare" treatment as `Schedule`/`DestinationCalendar`/`BookingPolicy`/`Availability`/`Attendee`/`OutOfOffice` above — none of these six domains get a column-level `Duplicate*Exception`; each gets only a `*NotFoundException`.
  - **`ResourcePricingEntity`**: single FK, `resource_id` → `ResourceRepository` (real). `price`/`effective_from` (`NOT NULL`, no default) get `@NotNull`; `currency` (`NOT NULL DEFAULT 'USD'`) is unannotated-but-guarded, same `@Builder.Default`-backed-optional pattern as `ResourcePricingEntity.currency`'s own entity-level note; `effective_until` (nullable) carries no annotation.
  - **`ResourceAssignmentEntity`**: three FKs. `resource_id` → `ResourceRepository` (real). `membership_id` → `MembershipEntity`, but **`MembershipRepository` doesn't exist yet** (only the entity does, same gap as `EcosystemRepository`/`TenantPlanRepository` before their own CRUD pass) — resolved via the bare reference-entity workaround (`MembershipEntity.builder().membershipId(request.membershipId()).build()`), *not* a real lookup; revisit once `MembershipRepository` gets its own CRUD pass, same as the standing `TenantServiceImpl` precedent. `assigned_by`/`released_by` → `UserRepository`, both nullable — resolved through a private `resolveUser(BigInteger)` helper returning `null` on a `null` id, same resolve-or-null pattern established on `BookingServiceImpl`/`WidgetOriginServiceImpl`. `assigned_at` is `@CreationTimestamp`-excluded from `ResourceAssignmentRequest`; `released_at` (nullable, no default) and `is_primary` (`DEFAULT true`, unannotated-but-guarded) are both on the request. This table has **no `updated_at`** column at all (only `assigned_at`), but still has real mutable columns (`membership`, `releasedAt`, `assignedBy`, `releasedBy`, `isPrimary`) on a surrogate PK, so the standard `update` verb is included — same reasoning as `AttendeeEntity`/`WidgetOriginEntity`.
    - **Skeleton file naming bug fixed**: the pre-existing DTO stub was named `RequestAssignmentResponse.java` (word order swapped) rather than `ResourceAssignmentResponse.java`. Since Java requires the public class name to match the file name, this would have forced the record itself to be misnamed `RequestAssignmentResponse` to compile. Renamed the file (deleted the old one, created `ResourceAssignmentResponse.java`) rather than preserve the typo — same category of fix as the `RefreshTokenEntity`/`@Table(name = "api_keys")` and `BookingServiceImpl implements AvailabilityService` skeleton bugs caught earlier; always verify a pre-existing DTO stub's filename matches its intended `<Name>Request`/`<Name>Response` convention before building on top of it, not just entity `@Table`/service `implements` targets.
  - **`ResourceCalendarEntity`**: two FKs, both real from the start — `resource_id` → `ResourceRepository`, `schedule_id` → `ScheduleRepository`. No columns besides the two FKs plus `created_at`/`updated_at`, but since both FK associations are themselves mutable, `update` is included (changing which resource/schedule a calendar link points to is a meaningful mutation, same reasoning as `ResourceAssignmentEntity` above).
  - **`ResourceMaintenanceEntity`**: single FK, `resource_id` → `ResourceRepository`. `start_time`/`end_time` (`NOT NULL`, no default) get `@NotNull`; `reason` (`TEXT NOT NULL`, unbounded) gets `@NotBlank` only, same treatment as `OutOfOfficeEntity.reason`; `status` (`VARCHAR(50) NOT NULL`, no default) gets `@NotBlank @Size(max = 50)` and stays a plain `String`, not a native enum, same look-alike treatment as `ResourceEntity.status`.
  - **`ResourceOverrideEntity`**: identical shape to `ResourceMaintenanceEntity` minus `status` — single FK `resource_id` → `ResourceRepository`, required `start_time`/`end_time` `@NotNull`, required `reason` `@NotBlank` only.
  - **`ResourceAttributesEntity`** — the one composite-PK (`@EmbeddedId ResourceAttributesId`) domain in this batch, modeled on the `UserRoleServiceImpl`/`RolePermissionServiceImpl` precedent: repository is `JpaRepository<ResourceAttributesEntity, ResourceAttributesId>` with no custom finder methods (`findById`/`existsById`/`delete` all take a constructed `new ResourceAttributesId(resourceId, key)`); `getById`/`update`/`delete` all take two params (`resourceId`, `key`) instead of one, and the controller exposes them as two path segments (`/{resourceId}/{key}`). Unlike `UserRole`/`RolePermission` (which have no mutable field and so skip `update` entirely), this table has a real mutable `value` column plus both `created_at`/`updated_at` (added later by `V74`), so **`update` is included** — first composite-PK domain in the codebase to combine a duplicate-check-on-create with a full `update` verb. `create` checks `existsById(new ResourceAttributesId(request.resourceId(), request.key()))` before building, throwing `DuplicateResourceAttributeException` on a hit — mandatory per the shared/derived-PK rule (`JpaRepository.save()` on a non-null composite `@Id` silently merges/updates instead of failing), same reasoning as `UserPasswordServiceImpl`'s `existsById` guard. `key` isn't a field on the entity itself (it lives solely inside the `@EmbeddedId`, per `ResourceAttributesEntity`'s own entity-level note), so `create` constructs the `ResourceAttributesId` directly (`new ResourceAttributesId(request.resourceId(), request.key())`) and sets it on the builder rather than letting `@MapsId` populate it — `@MapsId("resourceId")` only auto-populates the `resourceId` sub-field from the `resource` association at flush, not `key`. `resourceId`/`key`/`value` are all on `ResourceAttributeRequest` (`@NotNull`/`@NotBlank @Size(max = 100)`/`@NotBlank`) despite `resourceId`+`key` composing the PK — same precedent as `UserRoleRequest` carrying `userUid`+`roleId`: the exclude-the-PK DTO rule targets auto-generated surrogate PKs/UIDs, not client-supplied composite business keys.
- **`resource` bounded context CRUD layer now fully implemented** — all eight `resource` tables (`resource_types`, `resources`, `resource_pricing`, `resource_assignments`, `resource_calendars`, `resource_maintenance`, `resource_attributes`, `resource_overrides`) have complete controller/service/repository/DTO/exception stacks, not just entities.
- **`CustomerEntity` CRUD (`customer/customers/`)** — first `customer`-schema CRUD layer, built as the foundational domain since every other `customer` table FKs into it. Two FKs, both real lookups since `TenantRepository`/`OrganizationRepository` already existed: `tenant_id` → `TenantRepository` (`TenantNotFoundException`), `organization_id` → `OrganizationRepository` (`OrganizationNotFoundException`). `CustomerEntity` has **two separate composite-unique constraints** (`uq_customers_tenant_email` on `(tenant_id, email)`, `uq_customers_tenant_phone` on `(tenant_id, phone)`), same two-constraints-on-one-table shape as `ProfileEntity` — both get their own `existsBy*` on `CustomerRepository` (`existsByTenantTenantIdAndEmail`, `existsByTenantTenantIdAndPhone`, nested-path traversal through the `tenant` association into its PK) and both are checked in `createCustomer` before either FK is resolved, each throwing `DuplicateCustomerException` on a hit. `uq_customers_uid` gets no check — `uid` is `@UuidGenerator`-generated, never client-supplied, same treatment as every other generated-`uid` table. `email`/`phone`/`name` (`NOT NULL`, no default, bounded `VARCHAR`) get `@NotBlank @Size(max = n)`; `birthday` (`DATE NOT NULL`, no default) gets `@NotNull`; `status` (`VARCHAR(50) NOT NULL`, no default, plain `String` per its entity-level look-alike-not-a-native-enum note) gets `@NotBlank @Size(max = 50)`. `avatarUrl`/`notes` (`TEXT`, nullable) get no annotation; `locale` (nullable, no default, native enum) gets no annotation, same treatment as its own entity-level note; `timezone`/`gender` (nullable bounded `VARCHAR`) get `@Size(max = 100)`/`@Size(max = 20)` only. `marketingConsent` (`BOOLEAN NOT NULL DEFAULT false`) is unannotated-but-guarded (`if (request.marketingConsent() != null)` in both `create`/`update`), same `@Builder.Default`-backed-optional pattern as everywhere else. `metadata` excluded from both DTOs per the standing rule. `CustomerResponse` includes `customerUid` alongside `customerId`. `update` does not re-check either composite constraint, consistent with `ServiceEntity`/`SelectedSlotEntity` precedent (duplicate-check step is scoped to `create` only).
- **`CustomerTagEntity` CRUD (`customer/tag/`)**: single FK, `customer_id` → `CustomerRepository` (`CustomerNotFoundException`). `uq_customer_tags_customer_tag` on `(customer_id, tag)` backs `existsByCustomerCustomerIdAndTag` on `CustomerTagRepository`, checked in `createCustomerTag` before FK resolution, throwing `DuplicateCustomerTagException` on a hit — same nested-path-through-association shape as `CustomerRepository`'s own two `existsBy*` methods. `tag` (`VARCHAR(50) NOT NULL`, no default) gets `@NotBlank @Size(max = 50)`. Both `created_at`/`updated_at` present, so standard `update` is included.
- **`CustomerSourceEntity` CRUD (`customer/source/`)**: single FK, `customer_id` → `CustomerRepository`. `V46__customer_sources.sql` declares no `UNIQUE` constraint beyond its PK — same "don't invent constraints the migration doesn't declare" treatment as `Schedule`/`DestinationCalendar`/`BookingPolicy`/etc — so only `CustomerSourceNotFoundException` exists, no `Duplicate*Exception`, and `CustomerSourceRepository` has no `existsBy*` method. `source` (`VARCHAR(50) NOT NULL`, no default) gets `@NotBlank @Size(max = 50)`. Only `created_at` on this table (no `updated_at`), but `customer`/`source` are both real mutable columns, so `update` is included anyway — same reasoning as `AttendeeEntity`/`WidgetOriginEntity` (a missing `updated_at` column doesn't by itself disqualify the `update` verb, only a genuinely immutable/keys-only shape does).
- **`CustomerNoteEntity` CRUD (`customer/note/`)**: two FKs — `customer_id` → `CustomerRepository` (required), `author_user_id` → `UserRepository` but **nullable**, resolved through a private `resolveUser(BigInteger)` helper (returns `null` on a `null` id, otherwise `findById().orElseThrow(UserNotFoundException)`) — same resolve-or-null pattern established on `BookingServiceImpl`/`WidgetOriginServiceImpl`. `V43__customer_notes.sql` has no `UNIQUE` constraint, so only `CustomerNoteNotFoundException` exists. `content` (`TEXT NOT NULL`, unbounded, no default) gets `@NotBlank` only, same treatment as `OutOfOfficeEntity.reason`. Only `created_at` on this table, but `customer`/`authorUser`/`content` are all mutable, so `update` is included. `CustomerNoteResponse.authorUserId` is nullable and null-guarded (`customerNote.getAuthorUser() != null ? ... : null`), same shape as `WidgetOriginResponse.createdById`.
- **`CustomerActivityEntity` CRUD (`customer/activity/`)**: single FK, `customer_id` → `CustomerRepository`. `V44__customer_activities.sql` has no `UNIQUE` constraint, so only `CustomerActivityNotFoundException` exists. `activityType`/`referenceType` (`VARCHAR(50) NOT NULL`, no default) get `@NotBlank @Size(max = 50)`; `referenceUid` (`VARCHAR(255) NOT NULL`, no default) gets `@NotBlank @Size(max = 255)`; `description` (`TEXT`, nullable) gets no annotation. `metadata` excluded from both DTOs per the standing rule. Only `occurred_at` on this table (no `updated_at`), but `customer`/`activityType`/`referenceType`/`referenceUid`/`description` are all mutable, so `update` is included — same "missing `updated_at` doesn't disqualify `update`" reasoning as `CustomerSourceEntity`/`CustomerNoteEntity` above.
- **`customer` bounded context CRUD layer now fully implemented** — all five `customer` tables (`customers`, `customer_notes`, `customer_activities`, `customer_tags`, `customer_sources`) have complete controller/service/repository/DTO/exception stacks, not just entities.
- **`NotificationChannelEntity` CRUD (`notification/channel/`)** — first two `notification`-schema CRUD layers (built alongside `NotificationTemplateEntity`, see below). FK-free lookup table, same shape as `OrganizationEntity`/`TenantPlanEntity`. `uq_notification_channels_code` backs `existsByCode` on `NotificationChannelRepository`, throwing `DuplicateNotificationChannelException` on a hit. `code`/`name` (`VARCHAR(50)`/`VARCHAR(100) NOT NULL`, no default) get `@NotBlank @Size(max = n)`; `enabled` (`BOOLEAN NOT NULL DEFAULT false`) is unannotated-but-guarded, same `@Builder.Default`-backed-optional pattern as everywhere else. Only `created_at` on this table (no `updated_at`), but `code`/`name`/`enabled` are all real mutable columns, so the standard `update` verb is included anyway — same "missing `updated_at` doesn't disqualify `update`" reasoning as `CustomerSourceEntity`/`AttendeeEntity` above.
- **`NotificationTemplateEntity` CRUD (`notification/template/`)**: single **nullable** FK, `tenant_id` → `TenantRepository` (already real), resolved through a private `resolveTenant(BigInteger)` helper that returns `null` on a `null` request id and otherwise does a real `findById().orElseThrow(TenantNotFoundException)` — same resolve-or-null helper pattern established on `BookingServiceImpl`/`WidgetOriginServiceImpl`/`CustomerNoteServiceImpl`, applied here to a nullable FK that also participates in a unique constraint rather than a plain optional association. `uq_notification_templates_tenant_code` on `(tenant_id, code)` backs `existsByTenantTenantIdAndCode(tenantId, code)` on `NotificationTemplateRepository` (nested-path traversal through the `tenant` association into its PK, same shape as `ServiceRepository.existsByOrganizationOrganizationIdAndSlug`), checked in `createNotificationTemplate` before FK resolution, throwing `DuplicateNotificationTemplateException` on a hit — Postgres composite-unique null semantics (two rows with the same `code` and both `NULL` `tenant_id` don't collide at the DB level) aren't specially handled here, consistent with the project's standing practice of mirroring the declared constraint literally rather than reasoning about `NULL` edge cases the codebase hasn't dealt with elsewhere. `code`/`name`/`channel` (`VARCHAR(100)`/`VARCHAR(255)`/`VARCHAR(50) NOT NULL`, no default) get `@NotBlank @Size(max = n)` — `channel` stays a plain `String` per its entity-level look-alike-not-a-native-enum note, same treatment as `ResourceEntity.status`. `subjectTemplate` (`TEXT`, nullable) gets no annotation; `bodyTemplate` (`TEXT NOT NULL`, unbounded) gets `@NotBlank` only, same treatment as `OutOfOfficeEntity.reason`. `locale` (`public.locale NOT NULL`, no default) reuses shared `global/enums/Locale` with `@NotNull` — required-but-no-default, same treatment as its own entity-level note. `enabled`/`version` (`BOOLEAN NOT NULL DEFAULT false`/`INTEGER NOT NULL DEFAULT 1`) are both unannotated-but-guarded. `NotificationTemplateResponse.tenantId` is nullable and null-guarded (`template.getTenant() != null ? ... : null`), same shape as `WidgetOriginResponse.createdById`/`CustomerNoteResponse.authorUserId`.
- **`NotificationEntity` CRUD (`notification/notifications/`)** — built next (ahead of `NotificationQueueEntity`/`NotificationDeliveryEntity` in the request order) since both of those FK into it. Four required FKs, all real lookups since every target repository already existed: `tenant_id` → `TenantRepository`, `organization_id` → `OrganizationRepository`, `template_id` → `NotificationTemplateRepository`, `channel_id` → `NotificationChannelRepository`. `V54__notifications.sql` declares no unique constraint beyond `uq_notifications_uid` (generated, not client-supplied) — same "don't invent constraints the migration doesn't declare" treatment as `Schedule`/`DestinationCalendar`/`BookingPolicy`/etc — so only `NotificationNotFoundException` exists, no `DuplicateNotificationException`. `recipientType`/`locale`/`scheduledAt` (`NOT NULL`, no default) get `@NotNull`; `recipientUid` (`VARCHAR(255) NOT NULL`, no default) gets `@NotBlank @Size(max = 255)`; `status`/`priority` (`NOT NULL DEFAULT 'PENDING'`/`'NORMAL'`) are both unannotated-but-guarded, mirroring their `@Builder.Default`s. `subject`/`content` (`TEXT`, nullable) and `sentAt` (nullable `Instant`) get no annotation. `metadata` excluded from both DTOs per the standing rule. `NotificationResponse` flattens all four FKs to raw ids alongside `notificationUid`.
- **`NotificationQueueEntity` CRUD (`notification/queue/`)**: single FK, `notification_id` → the just-built `NotificationRepository`. `V56__notification_queue.sql` has no unique constraint beyond its PK, so only `NotificationQueueNotFoundException` exists. `availableAt` (`TIMESTAMPTZ NOT NULL`, no default) gets `@NotNull`; `status`/`attempts` (`NOT NULL DEFAULT 'QUEUED'`/`0`) are unannotated-but-guarded; `lastAttemptAt` (nullable `Instant`) gets no annotation. Only `created_at` on this table (no `updated_at`), but `notification`/`status`/`availableAt`/`attempts`/`lastAttemptAt` are all real mutable columns, so the standard `update` verb is included anyway — same "missing `updated_at` doesn't disqualify `update`" reasoning as `NotificationChannelEntity`/`CustomerSourceEntity` above.
- **`NotificationDeliveryEntity` CRUD (`notification/delivery/`)**: single FK, `notification_id` → `NotificationRepository`. `V57__notification_deliveries.sql` has no unique constraint beyond its PK, so only `NotificationDeliveryNotFoundException` exists. `provider` (`VARCHAR(100) NOT NULL`, no default) gets `@NotBlank @Size(max = 100)`; `providerMessageId` (nullable `VARCHAR(255)`) gets `@Size(max = 255)` only; `status` (`NOT NULL`, **no** SQL default here — unlike `NotificationEntity`/`NotificationQueueEntity`'s `status` columns) gets `@NotNull` rather than the guarded-optional treatment; `attempt` (`INTEGER NOT NULL`, no default) gets `@NotNull`; `errorMessage`/`sentAt` (nullable) get no annotation. Only `created_at` on this table, but every non-PK/non-timestamp column is mutable, so `update` is included. **Pre-existing skeleton bug fixed**: `NotificationDeliveryServiceImpl`'s stub was missing `implements NotificationDeliveryService` entirely (a plain `@Service` class with no interface) — same category of copy-paste-skeleton fix as `RefreshTokenEntity`'s `@Table(name = "api_keys")` and `BookingServiceImpl implements AvailabilityService` bugs caught earlier.
- **`NotificationPreferenceEntity` CRUD (`notification/preferences/`)**: single FK, `user_id` → `UserRepository`. `uq_notification_preferences_user_type` on `(user_id, notification_type)` backs `existsByUserUserIdAndNotificationType(userId, notificationType)` on `NotificationPreferenceRepository` (nested-path traversal through the `user` association into its PK), checked in `createNotificationPreference` before FK resolution, throwing `DuplicateNotificationPreferenceException` on a hit. `notificationType` (`VARCHAR(50) NOT NULL`, no default) gets `@NotBlank @Size(max = 50)` and stays a plain `String` per its entity-level look-alike-not-a-native-enum note. `emailEnabled`/`smsEnabled`/`pushEnabled`/`inAppEnabled` (`BOOLEAN NOT NULL DEFAULT false`) are all unannotated-but-guarded. `quietHoursStart`/`quietHoursEnd` (nullable `LocalTime`) get no annotation. `updatedAt` is excluded from `NotificationPreferenceRequest` per the standard `@CreationTimestamp`/`@UpdateTimestamp`-column-exclusion rule — this table's own entity-level note flags it as the first in the codebase with only `updated_at` and no `created_at` at all. `update` does not re-check the composite constraint, consistent with the `ServiceEntity`/`SelectedSlotEntity`/`NotificationTemplateEntity` precedent (duplicate-check step is scoped to `create` only).
- **`notification` bounded context CRUD layer now fully implemented** — all six `notification` tables (`notification_channels`, `notification_templates`, `notifications`, `notification_preferences`, `notification_queue`, `notification_deliveries`) have complete controller/service/repository/DTO/exception stacks, not just entities.
- **`MembershipEntity` CRUD (`organization/membership/`)** and **`MembershipRoleEntity` CRUD (`organization/membershiprole/`)** — closes out the `organization` bounded context CRUD layer (`organizations`/`profiles` were already done; these two were the entity-only gap flagged in `AGENTS.md`'s "Permission seed coverage" note). `MembershipEntity` is a standard surrogate-PK entity with two required FKs, both real lookups since `OrganizationRepository`/`UserRepository` already existed: `organization_id` → `OrganizationRepository`, `user_id` → `UserRepository`. `uq_memberships_user_organization` on `(user_id, organization_id)` backs `existsByUserUserIdAndOrganizationOrganizationId` on `MembershipRepository` (nested-path traversal through both associations into their PK, same shape as `ProfileRepository`'s own composite check), checked in `createMembership` before either FK is resolved, throwing `DuplicateMembershipException` on a hit. `accepted` (`BOOLEAN NOT NULL DEFAULT false`) is unannotated-but-guarded on `MembershipRequest`, same `@Builder.Default`-backed-optional pattern as everywhere else — no other scalar columns on this table. Both `created_at`/`updated_at` present, so the standard `update` verb is included (re-resolves both FKs, full-replace semantics) and does not re-check the unique constraint, consistent with the `ServiceEntity`/`SelectedSlotEntity`/`NotificationPreferenceEntity` precedent.
- **`MembershipRoleEntity` CRUD** follows the `UserRoleEntity`/`RolePermissionEntity` composite-PK precedent exactly (repository is `JpaRepository<MembershipRoleEntity, MembershipRoleId>`, no custom finder methods — `findById`/`existsById`/`delete` all take a constructed `new MembershipRoleId(membershipId, roleId)`). Two FKs resolved via real repositories: `membership_id` → the just-built `MembershipRepository`, `role_id` → the already-real `RoleRepository`. `create` checks `existsById` before resolving either FK, throwing `DuplicateMembershipRoleException` on a hit — mandatory per the shared/derived-PK rule, same as `UserRoleServiceImpl`. **No `update` verb** — same reasoning as `UserRoleEntity`/`RolePermissionEntity`: the table has nothing but its composite key and an immutable `@CreationTimestamp` (`assigned_at`), so there's no mutable field to update; only `create`/`getById`/`getAll`/`delete`. Controller exposes the composite key as two path segments (`/api/v1/membership-roles/{membershipId}/{roleId}`).
- **`organization` bounded context CRUD layer now fully implemented** — all four `organization` tables (`organizations`, `profiles`, `memberships`, `membership_roles`) have complete controller/service/repository/DTO/exception stacks, not just entities. Per `AGENTS.md`'s standing note, `MEMBERSHIP.*`/`MEMBERSHIPROLE.*` permission rows can now be added to a future seed migration since real endpoints exist to gate — this CRUD pass doesn't touch the permission seed migrations itself, that's a separate follow-up.

# Google OAuth2 — two mechanisms, three entry points

The server implements **two** unrelated Google integrations. Conflating them is the single easiest mistake to make in this area, so they are kept in separate packages with separate entry points.

| | Sign-In (authentication) | Delegated authorization |
|---|---|---|
| Question answered | who is this user? | may we act on their behalf? |
| Flow | ID token, posted by the browser | authorization code + redirect |
| Entry point | `POST /api/v1/auth/google` | `POST /api/v1/integrations/google/connect` |
| Google Cloud setting | Authorized **JavaScript origin** | Authorized **redirect URI** |
| Client secret used | no | yes |
| Produces | platform JWT pair | encrypted row in `integration.oauth_connections` |
| Token stored | none — verified then discarded | access + refresh, AES-256-GCM |
| Orchestration | `service/auth/DefaultGoogleSignInService` | `service/integration/DefaultGoogleConnectService` |

Both share `security/oauth/google/verifier/GoogleTokenVerifier` — the delegated flow reuses it to read the `id_token` that comes back alongside the access token, which is how the callback identifies which Google account granted consent.

**The third entry point is the one-shot registration flow**, and it is not a third mechanism — it is the delegated-authorization flow with the identity half of the same token response put to use instead of discarded. One authorization-code exchange already yields `id_token` alongside `access_token`/`refresh_token`, so registering and granting Calendar/Sheets/Drive happen on a single consent screen. See "One-shot registration" below.

Full architectural narrative lives in `SECURITY.md` sections 11–14. What follows is the server-side implementation detail.

## Endpoints

```
POST   /api/v1/auth/google                              permitAll   sign-in (ID token)
POST   /api/v1/auth/google/authorize                    permitAll   one-shot: returns authorizationUrl
POST   /api/v1/auth/google/handoff                      permitAll   one-shot: code -> JWT pair
POST   /api/v1/integrations/google/connect              Bearer      returns authorizationUrl
GET    /api/v1/integrations/google/callback             permitAll   302 back to the frontend
GET    /api/v1/integrations/google/connections          Bearer      owner-scoped list
DELETE /api/v1/integrations/google/connections/{uid}    Bearer      revoke at Google, then mark REVOKED
```

`/api/v1/integrations/google/callback` is in `SecurityConfig`'s `permitAll` list alongside `/api/v1/auth/**`. It must be: a top-level browser navigation from Google carries no `Authorization` header, so identity comes from the `state` parameter instead.

**`SecurityConfig` was not touched to add the two one-shot endpoints.** They live under `/api/v1/auth/**` specifically so they inherit the existing `permitAll` matcher — that URL choice is deliberate, not incidental. Putting them under `/api/v1/integrations/**` would have required a new rule.

## One-shot registration

Registration and delegated consent in a single Google round trip. **Login is unchanged** and stays on the GIS ID-token flow — the one-shot must send `prompt=consent` to guarantee a refresh token, and forcing a consent screen on every sign-in is hostile. A returning user who signed in via GIS but holds no `ACTIVE` connection should be nudged on the dashboard, not blocked.

```
POST /api/v1/auth/google/authorize          mode = SIGN_UP_AND_CONNECT, userUid = null
     PKCE + state -> Redis, 10m
     -> { authorizationUrl }

GET  /api/v1/integrations/google/callback   the same callback both flows use
  consume state (GETDEL)
  exchange code, verify id_token                       outside any transaction
  finalizeOnboarding(tokens, identity)                 @Transactional
      resolve-or-provision user  (GoogleAccountResolver)
      persist oauth_connection   (GoogleConnectionWriter)
      issue handoff code -> Redis, 60s
  -> 302 {frontend}{redirectAfter}?google=connected&handoff=<code>

POST /api/v1/auth/google/handoff            GETDEL -> userUid -> SessionIssuer
     -> { accessToken, refreshToken }
```

### Why a handoff code exists

The callback lands on Spring and redirects to the Next.js app, which owns the httpOnly session cookies. Spring cannot set a cookie for another origin, and putting tokens in the redirect query string would leak them into browser history, `Referer`, and every proxy log in between.

**Only the user reference goes into Redis — never the tokens.** The JWT pair is minted at exchange time. That makes the code in the URL bar a lookup key that dies on first use rather than a bearer credential with a TTL, and it means the 15-minute access token starts its life when the session actually begins rather than at callback time. `GETDEL` gives single-use consumption in one atomic command, same as `OAuthStateStore`.

### Components

| Component | Package | Role |
|---|---|---|
| `GoogleAccountResolver` | `service/auth/` | the three-step resolve-or-provision, **shared by both Google entry points** |
| `SessionIssuer` | `service/auth/` | `UserEntity` → JWT pair + persisted refresh token |
| `GoogleOnboardingService` | `service/auth/` | `finalizeOnboarding` (transactional) + `exchangeHandoff` |
| `SessionHandoffStore` | `auth/handoff/store/` | Redis, `oauth:handoff:<code>`, 60s, `GETDEL` |
| `GoogleConnectionWriter` | `service/integration/` | the `oauth_connections` upsert, split out of `DefaultGoogleConnectService.persist()` |
| `OAuthFlowMode` | `integration/oauthstate/model/` | `CONNECT` \| `SIGN_UP_AND_CONNECT` |

`GoogleAccountResolver` being shared is the load-bearing part. Registration and login are now two paths that can each provision a user; if they do not share resolution they will drift, and the thing that drifts is the `emailVerified` gate — an account-takeover control, not a formality.

`auth/handoff/` has **no entity, no repository and no table**, exactly like `integration/oauthstate/` — both are Redis-backed. Don't add a migration for either. This whole feature required **zero schema changes**.

### Traps specific to this flow

- **Branch on `mode == SIGN_UP_AND_CONNECT`, never on `mode == CONNECT`.** States written before the field existed deserialise with `mode = null` but still carry their `userUid`, so branching this way lets in-flight consents finish normally across a deploy. The inverse fails every consent started in the preceding 10 minutes. Covered by a regression test in `RedisOAuthStateStoreTest`.
- **Onboarding's default `redirectAfter` is `/google/complete`, not the settings page.** The callback returns *before* any session cookie exists, and the frontend gates `/dashboard` on one, so a `/dashboard/...` fallback would bounce the user to `/login` and discard the handoff code — silently, after a real consent. `DEFAULT_ONBOARDING_REDIRECT_AFTER` is separate from `DEFAULT_REDIRECT_AFTER` for this reason.
- **`finalizeOnboarding` is a separate bean, not a private method on `DefaultGoogleConnectService`.** Spring does not proxy self-invocation, so a private method would silently run with no transaction and a provisioning failure would strand a user with a Google credential and no workspace.
- **Watch the dependency direction.** `DefaultGoogleConnectService` → `GoogleOnboardingService` (the callback branch). `AuthController` therefore calls `GoogleConnectService.initiateOnboarding` **directly** rather than routing it through `GoogleOnboardingService` — the tidier-looking delegation is a constructor-injection cycle and fails at startup. `ServerApplicationTests` catches it if it is ever reintroduced.
- **A new connection with no `refresh_token` is skipped, not written.** `refresh_token` is `NOT NULL`, and on a brand-new row there is no stored value to fall back on — so the insert would abort the transaction that is *also* provisioning the account. Losing a calendar connection is recoverable; losing the registration is not. `DefaultGoogleConnectionWriter` guards this explicitly.
- **`status = ACTIVE` does not mean Calendar was granted.** Users can untick individual scopes and `persist()` sets `ACTIVE` unconditionally. Gate any "is this workspace calendar-connected?" question on the `scopes` array — raw Google URI strings, not `GoogleScope` enum names.

### Two pre-existing bugs fixed in the same pass

Both were live on the connect flow; the one-shot only made them user-facing on first contact.

- `DEFAULT_REDIRECT_AFTER` was `/settings/integrations`, which is not a route in the Next.js app. Now `/dashboard/settings/integrations`.
- `complete()` returned on `error` **before** consuming the state. Google sends `state` on error callbacks too, so this discarded the only record of where the user came from and left the state replayable for the rest of its TTL. The consume now happens first.

## Conventions this area deliberately breaks

- **The callback returns `ResponseEntity<Void>` with a 302, not a bare DTO.** Every other controller in the codebase returns a DTO plus `@ResponseStatus`. Here the caller is a browser mid-navigation, not an API client, so it must be redirected back to the dashboard with a `?google=connected|denied|invalid_state|error` outcome. `complete()` therefore never throws to the controller — it catches everything and converts it to a redirect URI.
- **`/connect` returns a URL for the client to navigate to, rather than issuing a 302.** The initiating call is an XHR with a Bearer header; `fetch` follows redirects transparently, so a 302 would make the browser try to *fetch* Google's consent page cross-origin and fail CORS. The navigation must be a deliberate `window.location` assignment client-side.
- **`OAuthConnection` CRUD is intentionally incomplete.** `POST`, `PUT` and list-all were removed from `/api/v1/oauth-connections`, along with their service methods and the `OAuthConnectionRequest` DTO (deleted outright — it carried client-suppliable `accessToken`/`refreshToken`). Only owner-scoped read and delete remain. This extends the existing "omit a verb the entity's shape doesn't support" (`UserRole`) and "omit a verb the domain's business policy forbids" (`audit`) precedents to a third case: **omit a verb whose existence is a security hole**. A row here holds live Google credentials, so the consent flow is the only sanctioned writer. Do not restore these endpoints for CRUD-completeness symmetry.
- **No `@Transactional` on the callback**, despite it writing to the database after a network call. On the connect path the write is a single row and `SimpleJpaRepository.save()` is already transactional, so the Google round-trip sits outside any transaction by construction. Adding an outer `@Transactional` would hold a Hikari connection open across a third-party HTTP call for no benefit. The onboarding path *does* need a transaction (it writes a user, an org, a membership, a profile, an account link and a connection), which is why it lives behind `GoogleOnboardingService.finalizeOnboarding` — the boundary starts *after* the Google calls, not around them.
- **Controllers must use `CurrentPrincipalProvider`, never `@AuthenticationPrincipal`.** `AuthenticationTokenFactory` stores `PrincipalUserDetails` (a wrapper) for users, so `@AuthenticationPrincipal UserPrincipal` binds to null silently. See `SECURITY.md` section 6.

## Google API details that bite

- Scope URIs need `www.`: `https://www.googleapis.com/auth/calendar`, not `https://googleapis.com/auth/calendar`. The latter returns `invalid_scope` at the consent screen.
- `drive.file` is used rather than full `drive` — full Drive access triggers Google's annual CASA third-party security assessment.
- Google returns granted scopes as a **space-delimited string**, not an array. Split before writing to the `TEXT[]` column.
- Google **omits `refresh_token`** on some re-consents. The column is `NOT NULL`, so only overwrite when one actually arrives; the stored token stays valid.
- `expires_in` is **relative seconds**, not a timestamp.
- `redirect_uri` must match **byte for byte** across the authorization request, the token exchange, and the Console entry.
- `client-authentication-method: client_secret_basic` means credentials go in the HTTP Basic header **only**. Also sending them in the form body returns `invalid_client`.
- Google's `iss` claim is sometimes `accounts.google.com` (no scheme). Spring's default claim converter parses `iss` as a `java.net.URL` and throws; `GoogleOAuthConfig` overrides that one claim to stay a raw String and accepts both spellings.
- The consent screen lets users **untick individual scopes**, so granted ≠ requested. Persist what was granted.

## Token encryption

`security/crypto/TokenCipher` + `AesGcmTokenCipher`: AES-256-GCM, fresh 12-byte IV per encryption prepended to the ciphertext, 128-bit tag, Base64 output. Keys come from `security.token-encryption.{current-version,keys}` and are validated at startup — wrong length or bad Base64 fails the boot, not the first request.

`decrypt(ciphertext, keyVersion)` takes the version as a parameter rather than reading it from the payload, which is what makes `oauth_connections.token_key_version` functional: rotation is a config change (add key 2, set current to 2), existing rows keep decrypting with the version recorded against them, and no re-encryption sweep is needed.

`TokenEncryptionException` is deliberately **not** wired into `GlobalExceptionHandler` — a decrypt failure is a server fault and cipher details must not reach a client, so it falls through to a generic 500.

## Test coverage

84 tests, all Mockito-based with no database and no network:
`DefaultGoogleConnectServiceTest` (17), `AesGcmTokenCipherTest` (13), `DefaultGoogleAccessTokenProviderTest` (7), `DefaultGoogleAccountResolverTest` (7), `RedisOAuthStateStoreTest` (7), `DefaultGoogleOnboardingServiceTest` (6), `DefaultCurrentPrincipalProviderTest` (6), `RedisSessionHandoffStoreTest` (6), `DefaultGoogleConnectionWriterTest` (4), `DefaultGoogleTokenVerifierTest` (4), `PkceGeneratorTest` (4), `DefaultGoogleSignInServiceTest` (2), `ServerApplicationTests` (1, full context load).

`ServerApplicationTests` is doing more work than its size suggests here: a full context load is what proves the `service.auth` ↔ `service.integration` wiring is a DAG rather than a bean cycle.

All 84 pass. One had been red since commit `123e971` and was fixed in this pass: `DefaultCurrentPrincipalProviderTest.requireUser_widgetActor_isForbidden` asserted the message contained `"requires a user actor"`, while `DefaultCurrentPrincipalProvider` throws `"Invalid actor: WIDGET"`. Never a behavioural defect — the right exception type was always thrown — so the **test** was aligned to the implementation, not the reverse. The production message names the rejected actor type, which is what makes the resulting log line diagnosable, and the assertion now checks for exactly that.

**Not covered:** a real consent round-trip against Google. Everything up to and including the token-endpoint call is mocked, so `GoogleTokenResponse` has never deserialised an actual Google payload — which now gates registration, not just calendar sync.
